# AKTUELLT — Werkz

> Living document. CC updates this at the end of every work session (see CLAUDE.md). Newest entry on top under "Log". "Now" is the single source of truth for what to work on next.

## Status: P1 daemon done → P2 — App MVP (moment loop live)

**Decided (locked):**
- Name: **Werkz**, domain werkz.app (registering)
- GDD v1.0 approved in full → `docs/gamedesign.md`
- Stack: Flutter (app, mobile-only portrait) / Node-TS (daemon) / CF Workers + Supabase (backend) / Astro (landing)
- Visual direction: 1955 industrial bureaucracy (riveted steel, manila folders, rubber stamps, CRT terminals). NOT neon-green hacker — that's a future paid theme.
- Layout: vertical cross-section workshop (Fallout Shelter format), building grows upward with progression
- Decision overlay: physical document slides up, swipe = stamp, <1s, haptics + stamp sound
- Payments: web-only Stripe, no Apple IAP (GDD §6.5)
- Art pipeline: gpt-image-2 via Fal (Rickard's skill + key in `.env`, never committed)
- Marketing: 1955 conglomerate that never breaks character (Liquid Death playbook), X as primary channel with build-in-public starting already in P1. Full plan in MASTERPLAN P2.5. **Launch-plan pivot (2026-07-20, task 29):** the build-story runs as a SERIAL posted post-completion (not live build-in-public), beta hardening proceeds in parallel, the launch video becomes the series finale; the waitlist gate is unchanged.

## Now (next 3 tasks, in order)

1. **Device gate (Rickard, hardware)** — build with `app/tool/device-run.sh` so the build is STAMPED (task 17 B); confirm the sha in Settings before reporting anything. Then, on a real phone: onboarding → camera QR pair, run a CC session, swipe-deny blocks, paste Haiku key → REAL narrated line + proof-of-life, file a work order → dispatched job appears. NEW: long-press the Settings title → LAYOUT TUNING panel; tune the spacing sliders live and read the numbers back to CC for hardcoding (task 17 C). Tasks 8–11 verified everything headless; the camera/LAN/valid-BYOK path is all that's left. See `app/DEMO-task9.md` + task-10/11 logs.
2. **P2 world layer** — Flame vertical workshop: worker sprites + ambient states, replacing the interim stacked rooms (building order canon: advisor penthouse / floor / archive basement). Poses approved, Rive chosen. **BLOCKED on Rickard's RIVE EDITOR SESSION** (est. 1–2 evenings): rig the three body-type artboards from the prepared parts + manifests (`assets-pipeline/sprites/parts/`), export `<persona>.riv` → `app/assets/rive/`. Steps in `.claude/handoff/task-19-rive.md`. After export, a short CC follow-up wires `worker_sprite.dart` + mounts sprites behind the debug flag.
3. **P1 narration weighting** — 80/18/2 routine/situational/golden classification in the daemon engine (currently narrates decision/session/key/job beats only; routine stays templated). Golden-moment weighting per GDD §3.

**Pre-beta (queued behind the 3 above, before any beta invite goes out):**
- Publish the real daemon as `werkz@0.1.0` — npx-runnable, which means solving the dist build first (bin currently points at `src/cli.ts`; needs compiled JS + shebang). The npm name is secured: `werkz@0.0.1` placeholder is live (2026-07-17), teaser only, no daemon source.
- Set up npm **trusted publishing** (OIDC from GitHub Actions) before beta — npm is restricting 2FA-bypass tokens Aug 2026/Jan 2027, so CI-based OIDC publishing replaces manual OTP publishes.

**P1 exit criteria (hardware, owner Rickard, personal dogfooding):** (g1) full multi-hour wall-clock hold released to execution; (g2) laptop sleep/wake across an open hold — verify on real hardware: with caffeinate holding idle-sleep (task 14 B1), does a forced sleep/wake (or lid-close on battery) drop the WS and recover cleanly, with pending decisions intact and the phone showing WORKSHOP UNREACHABLE then reconnecting? Not testable headless. Relay does not ship until both green.

**Done:** Task 1 (concept art) · Task 2 (event-model v0.2) · Task 3 (repo scaffold) · Task 4 (hold prototype — VIABLE) · Task 5 (event-model v0.3 + adapter slice) · Task 6 (daemon bootstrap) · Task 7 (LAN WS channel) — all 2026-07-16 · Task 8 (app moment loop) · Task 8b (first-pair crash fix) · Task 9 (first-run + ambient + BYOK narration) · Task 10 (pairing lifecycle + key feedback + stacked layout) · Task 11 (onboarding + UI fixes + work-order dispatch) · Task 11b (log drawer + mode teaching + floor crop + swipe/stamp rebuild) · Task 11c (pair-hang + black-camera fix) · Task 12 (regression sweep + screenshot discipline + app icon) · Task 13 (dispatch silent-failure fix + preflight diagnostics + UI polish) · Task 14 (always-on honesty + sleep handling + landing how-it-works) · Task 15 (bottom bar redesign + spacing round 2) · Task 16 (root causes: golden safe-area harness + replay≠live) — 2026-07-17 · Task 17 (decision lifecycle + build stamp + LIVE tuning panel) · Task 17b (per-room slot heights + draggable panel + advisor fix) · Task 17c (per-room FIT MODE — cover vs fitHeight) · Task 17d (signage moved to UI nameplates — the plate wasn't uniformly in the art) · Task 17f (revert nameplates → corner chips; crop advisor plate) · Task 18 A (codify final layout as device-independent ratios — LAYOUT SAGA CLOSED) · Task 18 B (worker sprite pose sheets — pose-approval gate) · Task 19 (sprite pipeline prep, Rive path — up to the editor door) · Task 19b (hi-res re-cut attempt — BLOCKED by source resolution, decision surfaced) · Task 19c (fix Rive input contract: mood is a number, not a string; pin the name contract) · Task 19d (regenerate hi-res masters via edit endpoint) · Task 19e (DIRECTION CHANGE: Rive dropped → Flame-native skeletal player + hi-res cutting) · Task 19f (mount SkeletalWorker behind the debug flag) · Task 19g (fix mount scale + position — root-caused) · Task 19h (fix part assembly — bind pose reproduces the master, root-caused) · Task 19i (walk locomotion + mirror-arm z-fix + joint simplification + speed defaults + defringe) · Task 19j (debug STATE forcer for tuning) · Task 19k (far-side duplicate fixes: no flip + depth offset/tint + prop removal + tuned defaults) · Task 19l (suite closer: corrected defaults + Checkwell & Sparkhand bundled + generated clipboard + 3-worker mount) · Task 20a (worker job-assignment + room-membership model) — 2026-07-18 · Task 20b (visual inter-room transit + room registry + dispatcher choreography + toast move) — 2026-07-19.

## Blocked / waiting

- Domain registration (Rickard)
- Fal key into `.env` (Rickard, local only) — done 2026-07-15

## Open questions

- ~~Room frames: baked steel borders differ slightly in corner radius between rooms.~~ **CLOSED 2026-07-17 (task 11.4):** crop the baked steel frames out of the room images (safe inset inside the frame, never touching room content/signage) and let the UI draw a single thin uniform floor-slab divider. Rooms sit nearly edge to edge; floor-name chips overlay the top-left corner. Corner-radius mismatch is moot once the frame is gone.

## Parked (do not touch until phase says so)

- Octagon implementation (P4), themes beyond #1 (P4), Hermes/OpenClaw adapters (v2), human PvP (v2), live spectatorship (v2)
- **Multi-workshop = paid boundary (P4):** projectId is repo-level (git identity), so each repo is one workshop. Free tier renders ONE active workshop and can switch between them; running multiple workshops at once is the paid feature (GDD §6). Free tier is always unlimited *within* one workshop (any number of workers/sessions in that repo).
- **Cloud Workshop tier (P4+ / post-launch):** we run the daemon (and Claude Code) on a managed machine for the user, so nothing on their desk has to stay awake — Rickard's Hetzner/VPS pattern turned into product. Removes the always-on-machine constraint entirely (task 14 makes that constraint honest in the meantime). Pricing intuition: priced ABOVE Foreman (relay) — you're paying for compute + uptime we host, not just connectivity. Landing FAQ + onboarding already point at it as a planned tier with a waitlist. Needs: managed provisioning, key custody model (BYOK on a machine we operate — trust/security design required before build), billing. Do not start pre-launch.
- **Assembly Line — chained agent pipeline (P4+, AFTER Octagon):** Advisor drafts PRD → planner agent → builder agents → reviewer validates diff against plan → verdict to Advisor → user's final stamp. Rendered as a literal 1955 factory line: the PRD travels by pneumatic tube from the penthouse down through the floors, refined per station, and returns for final approval. Reuses dispatch (task 13) + decision gates + plan-mode events. Build AFTER the Octagon — the Octagon's Agent SDK orchestration is the learning ground; a chained pipeline is 5 new failure modes and needs that foundation first.
- **Codex adapter (v2):** investigate whether the Codex CLI exposes a synchronous approval/hook surface (a PreToolUse equivalent we can hold on). If gateable → a full adapter (requisitions, holds, the whole loop). If not → a read-only PRESENCE mode: its events drive workers + narration, but it files no requisitions. Read-only mode is itself a feature — whole-team activity becomes visible even for agents we can't gate.
- **Lovable / cloud-agent presence (v3):** surface cloud agents (Lovable et al.) via GitHub webhooks — their commits appear as external contractor deliveries on the floor. Ambient only: no requisitions, no gating (we don't control their execution), just presence + narration of what landed.
- **Multi-USER workshops (undated / potential team tier):** two people paired to the SAME repo workshop (the Rickard+Dennis case). Open design questions: shared requisition visibility (both see the queue), per-user decision attribution (who stamped what), and trust ownership (whose trust decays when — is trust per-user or per-workshop?). Not in the current GDD monetization; a candidate team tier if the demand shows up. Distinct from Multi-workshop (that's one user, many repos).

## Parked (slice 2+ tuning)

- **Raise concurrent dispatch cap 1 → N once sprites land:** parallel jobs = multiple robots working simultaneously, visually rewarding; each job already carries its own workerId. Gate on the Flame sprite layer so the parallelism is legible on screen.
- **Rooms map to ACTIVITY TYPE, not repo areas:** more rooms come from workshop levels (GDD §4.2), not from the user's directory structure. Document this in the onboarding FAQ eventually so the mental model is clear.

## Idea backlog (first post-P2.5 feature sprint)

**HIGH-PRIORITY (the consult → dispatch → file closed loop):**
- **Advisor Consultation — chat-with-the-Advisor planning view.** A conversation with the Advisor NPC *before* dispatch, for planning a job. Runs on the user's LOCAL Claude Code in plan mode via the daemon — same auth as jobs, ZERO new keys. The Advisor's room (penthouse) becomes the consultation UI. Pairs with Form 22-C as a closed loop: **consult → dispatch → completion filing.**
- **Form 22-C "Completion Filing".** After a job exits with UNCOMMITTED changes, the daemon snapshots `git status`/diffstat and offers a swipe card (Haiku-written summary, dry bureaucratic tone): swipe RIGHT = commit + push as a follow-up step; swipe LEFT = changes remain on the machine, logged. Solves the observed device gap where "the agent did the work but never pushed." Reuses the decision-card swipe mechanics (task 17) + BYOK narration (§4.2). Sibling of task 23B's read-delivery (completion report) — reuses its capture + correlation plumbing.
- **Push notifications for pending requisitions.** "Decision pending" is already one of the four allowed notification types (event-driven, GDD hard rule) — this builds the delivery. (1) **P3/relay: APNs via backend** — daemon → relay → device when a decision opens; THE paid-tier differentiator ("your phone buzzes wherever you are"). Region-gating n/a (not payment steering). (2) **Nearer-term: local notifications** while the app is backgrounded-but-alive on LAN (WS event → local notif, zero server) — honest scope: dies when iOS kills the app; a bridge, not the product. (3) **Decision deep-link:** tapping the notification opens DIRECTLY to the swipe card (requisition overlay), not the home screen.

- **Standing Order (Form 12-A) — timed auto-approve as an issued document.** Per-category × workshop: "auto-approve routine commands for 2h" issued as a VISIBLE, revocable stamp (a posted standing order on the wall, revoke = tear it down). Designed AGAINST the trust curve (a standing order is a manual override lane, not a trust shortcut — no farming trust from auto-approvals, GDD §4.1 unchanged). Post-beta.
- **Apple multicast entitlement — SUBMIT ASAP.** The com.apple.developer.networking.multicast application takes weeks of lead time and costs nothing; it unblocks mDNS rediscovery (task 22's deferred DHCP-IP fix). Rickard files it; the code lands whenever it's granted.

**BETA-HARDENING (before any beta invite):**
- ~~**`.werkz/` credential leak.**~~ **DONE 2026-07-20 (task 37 A):** the state dir now self-ignores (`.werkz/.gitignore = *`, written on create + backfilled), 0700 dir + 0600 tokens, retires legacy narration-key, and warns loudly with the exact remediation if it's already tracked in git.
- **Hide the in-app debug tools.** The LAYOUT TUNING panel + WORKERS toggle + STATE forcer + sliders are gated on `kWerkzDebugTools` (`app/lib/src/debug_tools.dart`), shipped ENABLED so the release dogfood build (task 21) keeps them. Before beta: flip it false, or wire it to a build-time `--dart-define` (dogfood-on / beta-off), so real users never long-press into the tuning panel. `debugWorkerSprites` stays false regardless.

**PARKED (season polish):**
- **Ladder climb Workshop↔Archive.** A vertical transit animation (the worker climbs a ladder between storeys) replacing the side-edge transit for adjacent rooms. Season polish; parked (task 20b built the side-edge transit).
- **Big-robot close-up moments.** Special work-order events render a worker at large scale for a beat (the rig already supports per-worker height scale, task 19l). Parked.

## Log

- **2026-07-20 (task 37, P0 .werkz/ credential leak fixed + beam out of the intro card)** — A. P0 SECURITY — `.werkz/` LEAK: Rickard's `git add -A` staged `.werkz/{pairing-token,sessions.json,trust.json,daemon.pid}` and pushed them to a public repo; every Werkz user with a git repo had the same exposure. FIX (`state/state-dir.ts`, `ensureStateDir()` called FIRST at startup before any token write): the dir SELF-IGNORES — `.werkz/.gitignore = "*"` written on create AND backfilled into an existing unprotected dir (works regardless of the user's own .gitignore, without touching their file); dir locked 0700, token files 0600 (`saveJson`, pairing-token); a legacy `narration-key` is deleted (BYOK retired c270653 — verified NO code path writes it); and if `.werkz/` is ALREADY tracked in git (`git ls-files --error-unmatch`), a LOUD terminal box prints the exact remediation (`git rm -r --cached .werkz` + commit) and tells the user to UNPAIR + rotate + treat pushed tokens as compromised. Verified end-to-end: a real daemon start creates `drwx------ .werkz` + `*` gitignore + `-rw------- pairing-token`, and `git add -A` stages ZERO werkz files. 6 security tests (self-ignore, add-A-ignores-tokens, backfill, narration-key retire, tracked-warning fires, 0700). Ticked off the beta-hardening list. B. BEAM REMOVED from intro card 1: it didn't read well in the card, so `crew-beam.png` + all its layout plumbing (OverflowBox, stacking, height factor) are gone — the register stands alone as before 884907c, no dead widgets. The app asset + pubspec entry are removed; `beam_clean.png`, `beam-gen.mjs`, `beam-cut.py` and the auto-pickup guard STAY in the pipeline (season-polish return). first_run golden regenerated + viewed. daemon 122 tests (+6), app 171, analyze clean. SHA per part: A — this commit; B — the next.
- **2026-07-20 (task 36b, Rickard's hand-cleaned beam as a protected input; CC's isolate backed out)** — Rickard freed the beam himself; `beam-cut.py`'s per-column isolate is retired from the shipping path. `sprites/beam/beam_clean.png` (1161×215 RGBA, verified clean on a checkerboard) is now a PROTECTED auto-pickup input, same convention as `master-clean.png`: if present it is used VERBATIM (no isolate, no defringe), ALWAYS, ahead of any generated output → resolved to `crew-beam.png`. `beam-gen.mjs` skips generation entirely when it exists (never burns a gpt-image round, never overwrites it); `beam-cut.py` reads it only and NEVER writes it. NEVER-RESIZE GUARD: a `beam_clean.png` whose canvas ≠ 1161×215, or with no alpha, FAILS LOUDLY (proven: a 900×180 stand-in exits 1 with both sizes in the message) — rejected, not silently scaled. BAKED into first-run card 1 as `crew-beam.png`; layout kept from 884907c (register + full-width edge-to-edge beam via OverflowBox), only the height factor tracks the source aspect (215/1161 ≈ 0.185) so his art isn't squished. first_run golden regenerated + viewed. The beam is RICKARD'S hand-clean under rule 8 — never re-isolate over it. WORK SPLIT codified (CLAUDE.md standing rule): pixel cutout/freeing/simple image editing is Rickard's, CC builds code+pipeline; hand-cleans are protected `*_clean.png` auto-pickup inputs. analyze clean; 171 app tests. SHA — this commit.
- **2026-07-20 (task 36, bake in register + beam; alpha in the pipeline)** — Register + beam APPROVED and baked into first-run card 1. ALPHA moved into the pipeline (not manual): `beam-cut.py` (invoked by `beam-gen.mjs`, so the strip regenerates transparent every run) isolates the beam by PER-COLUMN silhouette fill — for each column, fill opaque between the topmost and bottommost metal pixel (lum > 42). This beats a flood (the gunmetal steel is near-black, so a flood ate through the body and left rivets floating); the per-column fill can't punch an interior hole or drop a rivet. Then the SAME defringe as the sprite cut (close→erode 1px→feather + RGB decontaminate) pulls the matte inside the dark fringe so there is NO black halo. Verified on a checkerboard (zoomed rivets + plate edge — clean; 88% opaque, transparent only in the corner notches around the taller brass plate). BAKED: cleaned register (beam-less, approved 34 A spacing) → `crew-register.png`; transparent beam → `crew-beam.png`; card 1 stacks the register (FittedBox-scaled to fit) + the beam FULL SCREEN WIDTH edge-to-edge (OverflowBox escapes the card's 20px padding). first_run golden regenerated + viewed. 171 app tests, analyze clean. SHA — this commit.
- **2026-07-20 (task 35, beam out of the register + rendered-metal beam element (both held))** — A. BEAM REMOVED from the register: the flat drawn girder inside the frame is gone; the register now ends with the figures standing on a thin floor line, nameplates directly under, nothing inside the frame. The APPROVED 34 A spacing is untouched. B. BEAM as a SEPARATE full-width element: the room art has no cleanly-cropable girder (mostly dark ceiling/pipes + floor), so it was GENERATED via gpt-image (edit endpoint anchored on the Workshop ceiling-beam crop for material match; 1536×1024, 3 candidates) — a horizontal riveted STEEL girder, rendered 3D brushed gunmetal with shadows/wear, DOMED rivets with real light+shadow, and a central aged-BRASS plate engraved "W · WERKZ · FACILITY OPERATIONS" — reads as the same 1955 world as the room art, not a UI widget. No neon. Auto-cropped to a full-width strip (`_beam-strip.png`, 1536×286) that spans EDGE TO EDGE (wider than the register). A card-layout mock (register + gap + full-width beam) was composed to show the intended in-card stacking. BOTH the cleaned register and the beam element are SHOWN and HELD — the app card asset is UNTOUCHED, awaiting Rickard's OK before baking (register replace + `crew-beam.png` add + card stacks them + first_run golden regen). Reproducible: `crew-register.py` (beam removed), `beam-gen.mjs` (raw attempts + ref gitignored, chosen strip kept). SHA — this commit.
- **2026-07-20 (task 34, register spacing + steel beam (held) + dedup pairing button)** — A. PERSONNEL REGISTER re-COMPOSED (from the existing front cutouts — no new gpt-image round): the wide Bolt/Sparkhand were nearly touching. Spacing is now EDGE-based with inter-figure gaps at 2× the end margins (≈90px vs 45px), so all three read as clearly separate with even air; added a riveted STEEL-BEAM floor girder with a central WERKZ plate anchoring the lineup; nameplates unchanged; 1955 palette, no neon. `crew-register.py` (reproducible) + `_crew-register.png` (candidate). HELD — the app card asset is UNTOUCHED, awaiting Rickard's OK (the bake was missed two rounds; not this time). Candidate image sent. SHA `dec4ca9`. B. DUPLICATED PAIRING BUTTON: the onboarding (pre-pairing) screen had an inline "SCAN THE PAIRING REQUISITION" button in the scroll content AND the pinned "PAIR WORKSHOP" bottom bar (task 26 C) — scrolling to the bottom showed two identical actions. Removed the inline one; the bar (Scaffold.bottomNavigationBar, its own region, not an overlay) stays and cannot hide the last text line — verified by a new bottom-scrolled golden (last credentials line + build stamp fully visible above the single bar). Goldens: `onboarding` (regenerated) + `onboarding_scrolled` (new). Tests updated (pairing_nav, onboard_smoke → pair via the bar; one-action assertion). 171 app tests, analyze clean. SHA — this commit.
- **2026-07-20 (task 33, codify tuned typing values + front-pose pose-gate cleared)** — A. CODIFIED Rickard's device-tuned typing values (rule 8, his — deliberate): typeHz 1.7→2.0, typeSwing 0.42→0.25, typeReach 1.2→1.25. Updated in SkelParams defaults + the tuned-defaults test; the shoulder-oscillation test threshold lowered to match the smaller tap (0.05→0.01 — still non-zero). Desk L/R HELD at 0.13/0.82/0.12/0.86 (Rickard hasn't landed those yet; they arrive separately). No CLAUDE.md values-list exists (rule 8 is the standing rule, no table); tuned values are recorded here. 170 app tests, analyze clean; type sliders sit below the panel golden's fold so no shipping golden moved. SHA `5dd98c6`. B. FRONT-POSE POSE-GATE — CLEARED, no blocker: the three front-facing personas (generated in 31 D via the gpt-image edit endpoint anchored on the approved pose sheets, `crew-front.mjs`) were re-verified as a proper pose-gate — isolated with the pipeline's `isolate()` and inspected on a checkerboard: CLEAN cuts (no halo, no background bleed, all feet/parts present), so NO hand-cleaning is needed and they AUTO-PICK. The PERSONNEL REGISTER candidate (front-facing Checkwell / Bolt / Sparkhand, 1955 nameplates, manila/steel, ON FILE stamp) is the same one baked into first-run card 1 under Rickard's 31 D authorization ("fixa rakt fram poser sen baka in dom"). Candidate image sent for the record; no new bake this round — awaiting Rickard's confirm or change requests (order/layout/regeneration). The 31 D side-PROFILE composite remains the documented fallback if he ever wants the fronts pulled.
- **2026-07-20 (task 32, typing pose to horizontal + workers land AT the benches)** — A. TYPING TOO STEEP: the +0.5 reach read as a ~45° diagonal-DOWN arm. Rendered a bias SWEEP (typeReach 0.2→1.4) with a horizontal guide and looked: horizontal/straight-forward needs a HIGHER angle, not lower (0 = arm hangs down, ~1.5 = horizontal) — flagged that Rickard's "lower it" and "make it horizontal" point opposite ways and followed the visual goal. `typeReach` is now a tunable SkelParams field, default 1.2 (near-horizontal), slider "type reach" (0–1.6). Tap swing cut to ~1/3 (`_typeSwingMax` 0.45→0.15 of typeSwing) and CLAMPED strictly below the bias (`min(0.15·typeSwing, 0.6·typeReach)`) so both shoulders stay forward of vertical at every phase — the task-31 A invariant test passes UNCHANGED (invariant wins over amplitude). Render strip viewed (arm reaches forward-horizontal toward the desk, both facings). SHA `514ca33`. B. WORKERS STOP TOO FAR FROM DESKS: desks were only on the workshop floor at 0.20/0.80 (left too far in; archive had none → typed mid-floor). Now desk arrival X is registry data PER ROOM PER SIDE `kRoomDeskX` — workshop [0.13 left terminal, 0.82 right workbench], archive [0.12 card-catalog, 0.86 right shelving] (advisor: none), placed AT the bench in the art; `desksFrom()` maps them to left-faces-left / right-faces-right workstations. LIVE-TUNABLE: `roomDeskXProvider` + per-room "desk L"/"desk R" sliders in LAYOUT TUNING so Rickard lands each worker on device, then the values are codified here (rule 8). EVIDENCE (live Flame can't golden): static placement composites per room×side (`_desk-previews.png`, viewed — worker AT each bench, not mid-floor). Tests: desksFrom mapping + registry-defaults-at-benches + a working worker walks to the NEAREST desk and faces it + no-desk falls back to in-place. SHA — this commit. 170 app tests; analyze clean; no shipping golden moved (desks/typing affect only live workers, off in tests). Trust/daemon/sprite-pipeline/report-rendering untouched. DEVICE: (A) OVERLAY on → TYPE forcer → the arm reaches horizontal-forward toward the desk; tune "type reach" if you want it flatter/steeper. (B) LAYOUT TUNING → workshop/archive "desk L/R" sliders → dispatch or force TYPE → the worker walks right up to the bench; read the four values back for codifying.
- **2026-07-20 (task 31 A-C, typing-arms fixed structurally + presence-gated room lighting + intro trim; D held for approval)** — A. TYPING ARMS BACKWARD (4th round of the arm class), ROOT-CAUSED FROM PIXELS: rendered a type strip and looked — the worker faces LEFT but the arm reaches RIGHT (away from the face) = behind the back. So the sign convention is the OPPOSITE of what 23→26 assumed: POSITIVE shoulder angle = FORWARD, NEGATIVE = behind. Idle only ever looked right because 29 landed it on exactly 0 (vertical); typing kept a NEGATIVE reach (−0.5) = arms behind, never caught. STRUCTURAL FIX (not a flip): typing is now `bias + swing` with the swing STRICTLY SMALLER than the bias — reach `_typeReach` = +0.5 (forward), tap `±0.45·typeSwing` ≤ ±0.45 < 0.5, so both shoulders (near+far) are mathematically > 0 (forward) at EVERY phase, even at the slider's max typeSwing — the backward excursion can never cross vertical (same immunity as 30 B's centered sway). Harness: both-facing strips now render for every anim (a facing-dependent arm bug can't hide left-only); a joint-space test asserts arm-upper AND arm-upper-far > 0 across the cycle at default AND max typeSwing. Viewed: typing reaches forward-down toward the desk in both facings. B. ROOMS LIT EMPTY (P0): a room lit from the event feed before its worker arrived. FIX: lighting is now GATED ON PRESENCE — pure `presentRooms()` (settled workers only; a mid-transit worker counts for NO room) + pure `nextLitRoom(intended, present, previous)` that lights the intended room ONLY when a worker is settled there, else holds the previously-lit staffed room, else a stable staffed room, else NOTHING (never lit+empty; property-tested that the chosen room is always in the present set). SYNC SPEED — a 3rd speed category above base/errand: a transit whose TARGET room has no settled worker RUSHES at `syncSpeedFactor` (default 2.5, slider "sync speed×") in BOTH the transit timing (transit_provider) and the visual x (worker_layer, `max(urgency, sync)`), so the light isn't delayed. WATCHDOG: home_screen logs loudly if the chosen lit room is ever empty (can't happen by construction — belt-and-suspenders, same spirit as the orphaned-worker vault). No shipping home golden moved (staffed-room cases unchanged; only the light-empty case is corrected). C. INTRO STEP 3 REMOVED: the task-30 "workshop narrates itself" card is deleted; the tour is 2 cards now, progress dots 3→2, ENTER-on-index-1; golden regenerated. D. INTRO STEP 1 DEAD SPACE — FILLED: Rickard asked for FRONT-FACING poses (the game sprites are all side-profile). Generated straight-on full-body portraits per persona via the gpt-image edit endpoint anchored on the APPROVED pose sheets (`crew-front.mjs` → `sprites/crew-front/<persona>/`, on-model identity kept), isolated with the pipeline's `isolate()`, composed into a 1955 "PERSONNEL REGISTER — FLOOR CREW" document (steel frame, manila header, nameplates, rotated ON FILE stamp) → `app/assets/images/crew-register.png`, baked into first-run card 1 as `_CrewRegister` (fills the former dead half). first_run golden regenerated + viewed (three front-facing robots, no dead space). TESTS: app 165 (+ typing-forward both-facing lock, presentRooms/nextLitRoom incl. never-empty property sweep, sync defaults; first_run + home_skel_forcer goldens regenerated + viewed). analyze clean. Trust/daemon/sprite-pipeline/report-rendering untouched. DEVICE: (A) OVERLAY on → a working worker's arms reach FORWARD toward the desk, never behind; (B) route activity to an empty room → the room stays dark until the worker (visibly rushing) arrives, then lights — never lit+empty; (C) fresh install → the tour is 2 cards.
- **2026-07-20 (task 30, urgency ACTUALLY applied + centered idle sway + desks + launch pair modal + BYOK killed)** — A. DISPATCH URGENCY, ROOT-CAUSED: 28's motion test passed because it drove the skeletal function directly with onErrand=true; the MOUNT fed the wrong flag. `onErrand` was keyed off `jobId`, which is NULL for the PRIMARY worker's interactive activity (documented 20a limit) — so the worker Rickard actually watches during his own session never sped up (his "zero difference"). FIX: onErrand now derives from the model's ENGAGED status (`working`/`walking`), not jobId — reliable and always set; ambient states (idle wander, coffee, return-home) stay at base, and a return-home transit (status coffee) is correctly NOT urgent because the flag reads the model status even while the display status is forced to walking mid-transit. PROOF is the overlay: it already shows an errand flag + EFFECTIVE px/s (task 29), so an engaged worker reads `E<~1.2×base>px/s` vs an ambler's `·<base>`. Mount-level test added (worker_model_test): dispatched→errand, primary-interactive-jobless→STILL errand (the fix), idle/coffee→not — closing the divergence class the skeletal-only test missed. B. CENTERED IDLE SWAY (Rickard): stationary arms get a small SYMMETRIC sway about exactly 0 (±`idleSway`, default 0.05, slider) — max forward excursion == max backward, both arms together. Centered symmetry is structurally immune to the 23→29 "behind the back" class (no directional bias to get wrong); harness check became symmetric-envelope-about-vertical, both facings, and `idleSway=0` still pins dead-vertical. C. WRITE AT WORKSTATIONS: `kRoomDesks` registry data (workshop L/R benches with arrival facing); a working worker walks to the NEAREST desk on the front plane and faces it before typing. Rooms with no desk type in place (no invented furniture). D. LAUNCH PAIR MODAL: a one-shot "WORKSHOP NOT CONNECTED — pair now?" dialog fires ~5s after a cold launch if still not connected — [SCAN QR]→scanner, [LATER]→workshop+banner; never when connected, never re-nagged on reconnect blips (single `_pairPrompted` latch). Goldens. E. BYOK NARRATION KILLED end-to-end: removed the key store, `/narration-key` endpoints, key UI (first-run card 3, settings block), app key plumbing (setNarrationKey/narrationKeyStatus/_syncNarration/narrationActive/last4), and the x-api-key Haiku call. Narration now spawns the user's OWN `claude` (`-p … --model claude-haiku-4-5 --output-format text`, the binary already resolved for work orders) — ZERO keys. Graceful: no claude / spawn error / non-zero exit / plan limit / timeout ⇒ no narration.ready, dry templates, logged ONCE dry ("workshop stays quiet"). narration.ready display + the LOG/report summary path are unchanged (source tag `haiku`→`claude`). Onboarding + settings copy rewritten ("Werkz needs zero keys"), landing FAQ line added, event-model §4.2 + GDD §6 + CLAUDE.md carry dated approved-deviation notes. TESTS: app 157 (+ mount-urgency chain, symmetric-sway envelope + idleSway=0 pin, desk-not-in-harness kept green, pair-modal appear/dismiss/never-connected, narration.ready still upgrades the log; goldens: settings/first_run/home_skel_forcer regenerated + viewed, pair_prompt new + viewed) + daemon 116 (narrate spawn happy-path/null-path/non-zero/throw+error, engine absence logs-once, with-claude emits source=claude). analyze clean; every SHIPPING golden that should be stable is (only settings/first_run/skel-forcer changed, intentionally). Trust routing + sprite pipeline + report rendering untouched. DEVICE: (A) OVERLAY on → dispatch OR run tools in your session → the engaged worker's label shows `E` + ~1.2× the px/s of any idle wanderer (screenshot both); (B) idle workers' arms sway gently, evenly around straight-down; (C) a worker starting to type first walks to a side desk and faces it; (D) launch with the daemon DOWN → after ~5s the pair dialog appears once; (E) with NO api key anywhere, narration lines still appear (your claude writes them) — kill claude/hit a plan limit and the workshop just goes quiet, no error.
- **2026-07-20 (task 29, vertical rest ends the arm saga + urgency diagnosis + bookkeeping)** — A. PLAIN VERTICAL REST (Rickard's call, closing the 23→25→26→28 family): every stationary state (idle, wander rest/dwell, coffee, post-transit, pre-job) poses BOTH shoulders at EXACTLY 0 — the authored bind hang, no per-persona bias, no sway, identical across personas. The whole bias apparatus is REMOVED (kIdleArmForwardDefault, PersonaSpec.idleArmForward, the animatePose param, the worker/layer plumbing); coffee's raised sip arm is retired with it (explicitly in the stationary list — the wind-down reads via head/torso/bob). Standing life = head nod + torso rock + body bob ONLY; walk/type keep their motion. Harness: the joint-space checks became EQUALITY-to-vertical (closeTo 0) across the full cycle, idle+coffee, near+far, and the strips (both facings, red pivot guide) document the look — deliberately boring; boring is the fix. B. URGENCY "ZERO DIFFERENCE" DIAGNOSED: the mechanism is test-proven at exactly 1.20× (28's motion test, still green) and jobId derivation is correct for DISPATCHED work orders (payload source='work-order' → jobId). Two honest explanations besides a stale build: (1) the PRIMARY worker's interactive activity carries NO jobId (documented 20a limitation) → base pace BY DESIGN — if Rickard watched Bolt during his own session, zero difference is expected; (2) at his tuned walkSpeed ~29, +20% ≈ 5.8 px/s — genuinely near-imperceptible. The OVERLAY now shows an errand flag + EFFECTIVE px/s per worker (`E34.8px/s` vs `·29.0px/s`), so the difference is MEASURED, not eyeballed; if it reads correct-but-subtle, the fix is the "dispatch speed×" slider, not code. C. BOOKKEEPING: backlog gained Standing Order (Form 12-A — per-category×workshop timed auto-approve as an issued/visible/revocable stamp, designed against the trust curve, post-beta) + Apple multicast entitlement (SUBMIT ASAP — weeks of lead time, zero cost, unblocks mDNS rediscovery); launch-plan pivot noted in Decided (build-story as post-completion serial, beta hardening parallel, video as finale, waitlist gate unchanged). TESTS: 152 app (arm tests rewritten to vertical-equality; strips regenerated + viewed: identical still arms in every stationary frame, both facings). analyze clean; no golden diffs (the overlay label is debug-only, off by default). Daemon/trust/walk-type untouched. DEVICE: install → workers stand with plain vertical arms, dead still (only head/torso/bob life), including during coffee and at POIs; OVERLAY on → dispatch a job → the assigned worker's label shows `E` + a px/s ≈1.2× the ambler next to it — screenshot the two labels side by side; if 1.2 is too subtle on screen, raise "dispatch speed×" and read the slider value back for codifying.
- **2026-07-20 (task 28, dispatch urgency 1.20× + still idle arms)** — URGENCY AS A LENS: workers with an ACTIVE JOB (model jobId) move at `dispatchSpeedFactor` (default 1.20, Rickard's number) through the SINGLE walkSpeedPx source — multipliers compose base × room × urgency via a new pure `effectiveWalkSpeed()` (and `effectiveWalkHz()` scales the cadence identically, so the stride speed/cadence ratio is invariant = no foot-slide; locked by test). APPLIES: job walks to the site (state-walk branch) + job-route transit LEGS (both legs multiplied in the worker_layer transitFrame call) + the walk cadence mid-transit (mount passes `onErrand` from jobId). NEVER applies: wander (branch reads the base speed regardless), return-home-after-coffee (jobId cleared on completion), patrol/forced states (no job on the mount). Slider "dispatch speed×" (1.0–2.0) in SKELETAL; default locked in the tuned-defaults test. STILL IDLE ARMS (Rickard's call, same task): idle arms now HOLD the per-persona forward bias — the ±0.03 breathing swing is gone; life comes from head bob + torso rock + body bob (locked: near+far angle constant across the cycle, still forward). TESTS: 152 app (+3: lens composition + stride invariance, errand-vs-ambient motion covers 1.20× ground over the same second, idle-arms-hold; tuned-defaults gained dispatchSpeedFactor 1.20). analyze clean; only the debug `home_skel_forcer` golden changed (new slider row) — every shipping golden byte-identical. Trust/daemon + animations beyond speed sourcing untouched. DEVICE: dispatch a job → the assigned worker visibly hurries (walk + transit) while idle wanderers amble at the old pace; idle workers stand with still, slightly-forward arms; tune "dispatch speed×" live if 1.20 reads wrong.
- **2026-07-19 (task 26, arms strike three + unpair modal + pairing UX + report cards)** — A. FACING HYPOTHESIS DISPROVEN EMPIRICALLY: the harness now renders every stationary state in BOTH facings (the exact device mirror: negative x-scale via facingScaleX, guide line mirrored too) — the right-facing render is a perfect mirror with the arm forward of ITS vertical; the flip preserves "forward" (matrix: M·R(θ)=R(−θ)·M mirrors positions AND rotations together). Permanent in the harness — a facing-dependent pose bug can no longer hide behind a left-only render. The hunt DID find a real member of the 23/25 bug class: **coffeeIdle set only the NEAR arm** — the far shoulder fell to `?? 0` = the authored behind-vertical hang, so every post-job wind-down stood with an arm behind the back; fixed (far arm gets the persona's idle bias) + a sweep test asserting EVERY stationary anim places BOTH shoulders explicitly, far never behind. CHECKWELL-FROZEN-MID-STRIDE: not reproducible in the harness (the 25 invariant + stationary-phase fix hold); most plausible = a still frame of a legitimate wander step, or a paused game widget — which is exactly what the NEW DIAGNOSIS OVERLAY resolves: LAYOUT TUNING gained an OVERLAY switch → per-worker floating label (persona · anim · L/R · shoulder angle, live from the real components) — the next screenshot self-diagnoses; if the label ticks it's not frozen, if it says walk on a planted worker we have the state, if idle with a wrong angle we have the math. B. UNPAIR MODAL DEAD, root-caused: `revokeSession` had NO timeout — with the daemon asleep/off-network the awaited revoke hung FOREVER after confirm, so the dialog "did nothing" (the 24 widget test passed because localhost refuses instantly). Fix: revoke is now FIRE-AND-FORGET with a 3s cap — the local wipe is the user's decision and happens immediately; locked by a test where revoke literally never completes. C. PAIRING WHERE THE USER IS: OnboardScreen got a PINNED "PAIR WORKSHOP" button (bottom bar — the old CTA sat below the scroll fold) → scanner directly; the UNREACHABLE banner keeps auto-reconnect as the primary line and gained a quiet RE-PAIR text button (opens the scanner; scanning a fresh QR replaces the pairing — never auto-degrade, 24's invariant intact and still test-locked). Goldens regenerated + viewed: onboarding, home_unreachable. D. DESIGN ANSWER (advisor wander): wander CANNOT enter the Advisor — POIs are in-room data, advisors-office has none, and wander never crosses rooms; what Rickard saw is MODEL routing (advisor-tagged events place the event's worker there; un-jobbed activity routes to Bolt) with the worker LINGERING after the beat. Recommendation: KEEP the advisor excluded from wander (a close-up room should be staffed only on business); the lingering wants the 20b return-home choreography extended to un-jobbed advisor visits — backlogged, one line, not built now. E. REPORTS ONE TAP: toast-tap verified working by the existing 23 test (the mechanism was never broken — the 2.5s self-dismiss window is just easy to miss, which is why E2 exists); NEW unread-report card: a live completion with a report now surfaces a small manila FILED-DOCUMENT card (Form 9-R, red unread dot, slightly askew, bottom-right above the bar — "awaiting pickup", not a notification bubble); one tap opens Form 9-R and clears it; toast-open also clears it; LIVE-only per the 16 B pattern (replay never resurrects it; LOG stays the durable archive — locked by test). Golden home_report_card viewed. TESTS: 149 app (+4: both-shoulder stationary sweep, unread-card state live/replay/clear, card one-tap widget flow, hanging-revoke unpair; both-facing strips fold into the existing harness tests). analyze clean. Daemon + sprite pipeline untouched. DEVICE: install → LAYOUT TUNING → OVERLAY on → screenshot the standing workers (labels tell all: anim/facing/angle) → unpair confirm now works (and is instant even with the daemon down) → onboard shows the pinned PAIR WORKSHOP → dispatch an audit → let the toast expire → the Form 9-R card waits bottom-right, one tap opens it.
- **2026-07-19 (task 27 CRITICAL, trust 0 blocked everything + "ask" never asked — three layers root-caused)** — (1) WHY TRUST 0: the TrustStore was MEMORY-ONLY with default 0 ("persistence lands in P2") and NOTHING in the codebase ever raised it — not task 24's doing (there was never a store to orphan; cli.ts even passed `trust = undefined`). Every daemon start = every worker at 0, forever; Rickard's "days of history" had accumulated nothing anywhere. NOW: trust persists per WORKSHOP in `.werkz/trust.json` (keyed by workerId, untouched by pairing — locked by a re-pair test), and the GDD §4.1 immediate **+1 per phone-approved decision** is live (new trust rides the decision.approved payload; revert −5 / expiry −1 stay P2 pending revert detection). (2) WHAT TRUST 0 PERMITS: reads auto-allow from 0 (threshold 25→0 — GDD §4.1's own "starting values; calibrate" sanction, noted in GDD Open Questions §9.2): reads are side-effect-free and gating them made every fresh workshop reject pwd/ls/Read. Ladder now starts at routine (50) / small-diff (75); destructive NEVER auto (unchanged hard line). (3) "ASK" NEVER ASKED — the core bug: below-threshold routes returned `permissionDecision: "ask"` straight to the CC hook ("CC's own dialog handles it — we do not hold", §3 as designed). That assumed an interactive terminal; a HEADLESS `claude -p` work-order job has no dialog → CC turned every "ask" into an instant silent deny, resolved in ~0.02ms, no requisition ever raised — which is exactly Rickard's audit log. FIX: 'ask' REMOVED from routing (type-level); below-threshold HOLDS = a requisition on the phone under the §3.4 adaptive ceiling; the ceiling elapsing replies 'ask' (interactive fallback = CC's dialog, per the original §3.4 walk-away design) with a reason that tells the agent "awaiting operator approval — NOT a refusal". (4) LOUD-FAILURE FOR THE AGENT: all hook reasons rewritten to instruct — operator-denied says "do not retry; note the denial in your report", superseded says "continue other work and report awaiting approval", dedup echoes the operator's recent verdict. event-model §3.1 table + routing notes REVISED with a dated approved-deviation block. TESTS: daemon 114 (+8: trust persists across restart, re-pair never touches trust, increment clamps, read@0 allows, low-trust routine holds→phone→release, no-phone hold supersedes with awaiting-wording, +1-on-approve rides the event, deny-wording; router: below-threshold holds + a sweep asserting NO class×trust combo ever yields a silent outcome). App suite untouched (145 green). DEVICE: restart the daemon (`werkz` — picks up the new routing), re-run the audit dispatch "give me an audit of what is in root": expect reads to auto-flow (LOG shows task.started lines), any routine/write steps to raise requisition cards on the phone, and a Form 9-R with ACTUAL findings; approve a few requisitions and check Settings/log — `.werkz/trust.json` should now exist and grow, and survive both a daemon restart and an unpair→re-pair.
- **2026-07-19 (task 25, idle-arms regression — TWO real device bugs found, neither the suspected one)** — The prime suspect (wander REST not driving the fixed idle) was INNOCENT: code-reading + the new state-driven harness prove every standing path resolves to a named animation every frame. The actual mechanisms: (1) **AMPLITUDE, not code path** — 23's single −0.12 bias was SMALLER than the authored bind-pose hang on Bolt/Sparkhand, so the net arm still sat behind vertical and the breathing read as backward rocking; the 23 harness verified "more forward than before", never "forward of the VERTICAL" (that was the real harness gap). Measured against a red vertical guide through the shoulder pivot (now baked into every strip frame): Checkwell's clipboard arm was already forward at −0.12, Bolt/Sparkhand were not → the bias is now PER-PERSONA on PersonaSpec (Bolt −0.30, Checkwell −0.12, Sparkhand −0.35), threaded worker_layer → SkeletalWorker → animatePose. (2) **dwell/rest MARCHED THE WALK CYCLE in place** — the wander's arrival tolerances (2px / 0.03 depth) were WIDER than the anim's moving-thresholds (1px / 0.01), so a worker the choreography had declared arrived could keep playing walk while visually planted: ±armSwing shoulder swings on a stander = exactly "standing mid-room rocking arms behind the back". Fixed at the source: rest/dwell are stationary phases and NEVER play walk (the ≤2px residual glides out under idle, invisible). BONUS QUIRK fixed: a pristine `Wander()` (timer 0) picked a POI on frame one — every worker started drifting at mount instead of resting first (spec order rest→POI); an unseeded rest now seeds its lazy interval. RULE-OUTS: SkelParams are not persisted (panel is per-session) and the bias is a const/spec value outside SkelParams — no stale-tuning override possible; the far group receives the bias via the joints map (in-strip). INVARIANT: `lastAppliedAnim` records the named animation applied each placed frame (+ debug assert) — no state may leave the skeleton unposed; checked per-frame by the harness. HARNESS CLOSED TWO WAYS: (a) phase_render_test now ALSO drives the DEVICE state machine (AUTO + wander rest/toPoi/dwell/home-walk/post-rest per persona → `_phase-state-driven.png` strips, gitignored, viewed: all stationary frames forward of the guide on all three personas); (b) discovered that a TestFailure thrown INSIDE `runAsync` in a plain `test()` can be SWALLOWED (the test showed green while its expects failed!) — the harness now collects inside the closure and asserts OUTSIDE it. TESTS: 145 app (+4: 3 state-driven strips with per-frame invariant checks, pristine-rest seed; per-persona forward sweep folded into the harness). analyze clean; no golden diffs. Walk/type/coffee animations + all tuning defaults untouched; task-24 files untouched. DEVICE: install, watch an AUTO worker stand mid-room ≥60s (through at least one wander rest): arms slightly forward, breathing, NEVER behind the back — including right after it walks home from a POI (the old marching window) and directly after app start (workers now rest before their first drift).
- **2026-07-19 (task 24, session resilience: the 68s self-close, report-in-replay, pairing UX)** — Diagnose-first on all four. (1) REPORT IN REPLAY — NO DROP EXISTS in current code: a new end-to-end test (`report-replay.test.ts`) runs Rickard's EXACT sequence — pair → dispatch → job completes with report → daemon RESTART (fresh EventBus/PairingManager over the same state files) → phone re-pairs with a FRESH QR → hello replay — and the report arrives INTACT (regression-locked; the app half was already locked by 23's replay test). Most plausible device explanation: the completed order he inspected was written by a pre-23 daemon process (no `report` field on that event — `werkz` runs from checkout, needs a restart to pick up new code), while the "1830 chars" daemon log line came from a later completion. To make the next diagnosis instant, the WS hello now LOGS what it replays: `WS hello: replaying N events (M carry reports)`. (2) THE 68s SELF-CLOSE — root-caused structurally: the ONLY code path that sends a clean close(1000) is `DaemonClient.dispose()`, and dispose fires ONLY when the pairing state changes (pair/unpair) — connection loss can NEVER route to the pairing screen (client code has no reference to pairing storage; locked by a new invariant test: repeated socket loss → workshop + UNREACHABLE banner, never onboarding). The one flow matching ALL the evidence (healthy WS closes 1000 · pairing screen · daemon reissues a fresh token, making the on-screen QR stale · six rejected scans): the UNPAIR button — a bare, unconfirmed red TextButton directly under REPLAY INTRODUCTION. It now CONFIRMS first (dialog explains it voids the current QR). REINSTALL: `device-install.sh` overwrite-install does NOT wipe pairing — flutter_secure_storage is iOS keychain, which survives reinstall (same team/bundle); "unpaired at start" is explained by the unpair, not the reinstall. (3) REJECTED-PAIR FEEDBACK — the reason WAS shown but got WIPED within a frame: on failure the scan handler reset `_lastRaw`, the camera (still pointed at the QR) resubmitted on the next frame, and `_submit`'s "pairing…" status overwrote the error — an infinite wipe loop = Rickard's six silent scans. Fix: a failed raw is HELD (same code never auto-resubmits; error stays on screen; a DIFFERENT/fresh QR pairs instantly; explicit SCAN AGAIN button rescans the same one), + daemon reasons map to actionable phone copy ("This pairing code was already used — run `werkz qr` on your computer for a fresh one."); golden `pairing_rejected` (viewed). (4) CREDENTIAL AUDIT — already correct, confirmed: WS reconnects auth with the SESSION token (`/ws?token=<session>`, verified against the sessions set, never the one-time pairing token); sessions persist to `.werkz/sessions.json` (loaded on daemon start, saved on pair/revoke) so a daemon restart orphans no phone. One nuance flagged: the pairing token is pinned across restarts and `#pairingUsed` resets on start — a USED QR becomes scannable again after a daemon restart (deliberate convenience; the printed QR keeps working — noting, not changing). TESTS: app 141 (+4: loss-never-routes-to-pairing invariant, unpair-confirm flow, friendly-copy map, reject golden), daemon 106 (+1 e2e replay). analyze clean; all pre-existing goldens byte-identical. Sprite pipeline, Form 9-R, task-22 backoff untouched. DEVICE RE-ENACTMENT: fresh QR (`werkz qr`) → pair → dispatch "audit root" → wait for COMPLETED · REPORT FILED → RESTART the daemon (`Ctrl-C`, `werkz`) → app shows WORKSHOP UNREACHABLE then reconnects on its own (NEVER the pairing screen) → LOG → the order's "▸ REPORT" line still opens the full report (daemon log should read "replaying N events (1 carry reports)") → Settings → UNPAIR now asks first → scan the now-stale QR → the already-used message STAYS on screen → `werkz qr` → fresh QR pairs.
- **2026-07-19 (task 23, idle arm diagnosis + work-order result delivery)** — A. IDLE ARMS, DIAGNOSED FIRST: (1) "20c-fix" does NOT exist — no commit, branch, stash, or AKTUELLT entry anywhere; the type/coffee forward signs (−0.5/−0.9) have been unchanged since 19i, and the tree was CLEAN at session start (task 22's flagged uncommitted work all landed as 5fcff23, which touched reconnect+flags, not animations). So whatever session produced "20c-fix" never reached the repo — nothing to reconcile, flagged here. (2) The actual gap is an OMISSION, not a sign error: idle set NO shoulder angle, so arms rested at the authored bind pose — which on ALL THREE personas hangs the arm at/behind vertical — and the torso micro-rock (which the far group doesn't even inherit) made it read as rocking behind the back. VERIFIED with a NEW phase-render harness (`phase_render_test.dart` — renders the real assembly at quarter-phases of idle/walk/type/coffee per persona → `_phase-*.png` strips, gitignored; walk/type/coffee confirmed correct, contralateral + forward reaches). FIX: idle now biases BOTH shoulders slightly forward (`_idleArmForward` −0.12 + 0.03 breathing, always negative), locked by a full-cycle sweep test (near+far strictly forward at every phase). B. WORK-ORDER RESULT DELIVERY (the missing report): the daemon now captures the `result` field of `claude -p --output-format json` (the closing answer, not the transcript), caps it at 16KB with a LOUD truncation marker (`capReport`, exported+tested), and attaches it as `report` on the job.completed payload — device-zone like diffCore (§4.3): never in narration facts (the Haiku allowlist is untouched), never on a share surface. NOTE the same looseness diffCore already has: bus events persist to `.werkz/events.jsonl` on the user's own machine, so the report text lands there too — pre-existing pattern, flagged not silently redesigned. APP: `WorkOrderStatus` gains report+completedEventId; the completed toast reads "COMPLETED · REPORT FILED" and tap OPENS the report (banner dismisses; report stays reachable); LOG lines for job.completed with a report get a bold "▸ REPORT" affordance and open it too (works for replayed history — the durable path after the toast's 2.5s self-dismiss). REPORT VIEW (`work_report_sheet.dart`): a filed 1955 document — manila letterhead "COMPLETION REPORT — FORM 9-R", rotated FILED stamp, LABOR EXPENDED meta, the Haiku one-liner as foreman's summary (narration[completedEventId], stock line fallback), body = selectable monospace on cream paper. RELATION TO FORM 22-C (backlog): 23B is READ-delivery only; Form 22-C is the ACT-delivery sibling (swipe to commit/push uncommitted changes) and reuses this exact capture+correlation plumbing when built. TESTS: daemon 105 (+3: report captured on the right completed event, no empty report key, 16KB cap loudly marked); app 137 (+11: idle-arm full-cycle lock, 3 phase-strip renders, report fold live+replay, toast-opens-report, LOG-opens-report, 3 report goldens SE/12/ProMax — viewed, clean, no overflow). analyze clean; every pre-existing golden byte-identical. Sprite pipeline + reconnect untouched. DEVICE STEPS: (A) install, watch any idle worker — arms should hang slightly forward and breathe, never swing behind the back (check all three personas + after a wander walk ends); (B) with the daemon running, dispatch "give me an audit of what is in root" → toast COMPLETED · REPORT FILED → tap → the full answer as a filed report; let the toast expire instead → LOG → tap the "▸ REPORT" line; kill and restart the app → LOG still opens the report (replay).
- **2026-07-19 (task 22e, Bolt re-cut with updated leg override)** — Task said leg-lower, but the file that actually changed on disk was `parts-clean/leg-upper.png` (37888→64564 bytes; leg-lower byte-untouched since 22d) — flagged to Rickard in the report (either the task text or the saved file is the mixup; if the intended fix was leg-lower, it hasn't landed). Single-persona re-cut (`cut-hires.py 7a19`) picked up both leg overrides (cut-log confirms), both far legs pixel-identical to their current parts-clean files, bind-pose IoU 0.906 (eyeballed clean), Bolt's four leg PNGs re-bundled, 126 app tests green. Commit 4abf891's message says leg-lower (written before the discrepancy was caught); the follow-up commit carries the actual updated leg-upper source + this correction. Nothing else touched.
- **2026-07-19 (task 22d, re-cut with Rickard's parts-clean overrides active — first real use of the 22b layer)** — Five hand-cleaned leg parts landed (7a19 leg-upper+leg-lower, 3c57 leg-upper+leg-lower, 9b72 leg-upper); all five passed the dimension guard (exact region-size RGBA), picked up VERBATIM (cut-log per persona lists them; provenance on each bindpose-diff footer) and PROPAGATED: far-leg duplicates pixel-verified identical to Rickard's files. IoU: Bolt 0.950→0.905, Checkwell 0.726→0.710, Sparkhand 0.884 flat — the dips are the HONEST cost of despilling: the machine foot crop had the far foot's dome baked in as spill, which happened to fill the master's far-foot silhouette; now the real far duplicate carries it (behind the near leg, depth-offset) so the reassembly no longer fakes coverage — renders eyeballed cleaner than before (single boot per foot, no ghost dome). EDGE CROPS (3×, same framing as 22c): Bolt's leg parts now fully clean; Checkwell's clean bar a thin strip at the leg-lower right edge; Sparkhand's leg-upper KEEPS the brown glove — likely deliberate (the arm hangs in FRONT of the thigh in the master, so erasing it would punch an occlusion hole needing inpaint) but it's baked into the leg, so it will swing WITH the leg in walk — Rickard judges from the crops whether it needs an inpaint pass. Re-bundled all three to app assets; 126 app tests green; no golden diffs. No manifests/app code/master-clean/clipboard touched.
- **2026-07-19 (task 22, workers on by default + reconnect resilience — instrumentation + hardening)** — (A) WORKERS ON: `debugWorkerSprites` const → true; the WORKERS toggle stays as an off-switch. HONEST LIMIT: a live Flame GameWidget can't be golden-captured (continuous ticker → pumpAndSettle hangs; async sprite decode never finishes in the captured frame — demonstrated 3×), so the promotion is the const flip, DEVICE-verified; every HomeScreen test disables the worker layer (WorkersOffController override) to stay deterministic. Only `home_layout_tuning` golden changed (the new CONNECTION readout); all others byte-identical. (B) RECONNECT — the explorer found most pieces already existed (client 20s ping, backoff, replay-from-eventId, unreachable; daemon 30s heartbeat + terminate, mDNS advertise). GAPS CLOSED: INSTRUMENTATION (the "stop guessing" priority) — the daemon now logs WHY a socket ended (close code/reason, which layer: peer-close / error / heartbeat-sweep, + uptime; injectable `log` seam); the app captures the close reason + connection stats (uptime, drops this session, last drop reason, last-pong age, foreground) via `ConnStats`, surfaced in a new CONNECTION section of the debug panel. HARDENING (safe regardless of root cause): (a) CLIENT-SIDE silent-drop detection — the client pinged but never checked pongs; now 2 missed pongs (≈40s: NAT/power-save death with no close frame) force a reconnect in seconds; (b) backoff retuned to 1s→30s cap with ±30% JITTER + an app-lifecycle observer so FOREGROUND (and `networkChanged`) kicks an immediate retry instead of waiting out the backoff; (c) replay-from-eventId confirmed already correct. TESTS: 126 app (+4: backoff schedule, jitter bounds, close-reason strings, fake-socket heartbeat silent-drop→reconnect via fake_async) + 102 daemon (+1: close code/reason logged with the layer). analyze clean; flag TRUE (workers shipping). **DEFERRED (flagged, not done): (d) mDNS REDISCOVERY on >60s failure (browse `_werkz._tcp`, match a workshop-id in the TXT, update the stored host:port — the permanent DHCP-IP-rotation fix) + (e) the unreachable banner "searching…" state.** These are the largest new subsystem: app mDNS BROWSING needs the `multicast_dns` dep AND the iOS **multicast networking entitlement** (Apple-gated — a real request, not just code) + a workshop-id in the daemon's mDNS TXT. Per the task's own "diagnose first" sequencing, instrumentation ships now so Rickard reads the drop reasons over one evening; rediscovery is the next increment (gated on that diagnosis + the entitlement). No pairing/QR/token/shipping-UI changes beyond the const flip + the CONNECTION readout; assets-pipeline untouched (Rickard's in-flight 22c work).
- **2026-07-19 (task 22c, all three personas re-cut from Rickard's full-res master-clean files)** — DIMENSION GATE first: every master-clean is 1024×1536 RGBA, exactly matching its attempt1-1 source (Bolt's earlier 408×612 export mishap replaced) — none stopped. Cut with auto-pickup for all three (cut-log: master=master-clean, overrides none), re-bundled all 33 part PNGs to app assets, bind-pose re-rendered + diffed. IoU: Bolt 0.950→0.950 (flat — the res bump doesn't move silhouette agreement), Sparkhand 0.889→0.884 (flat), Checkwell 0.780→0.726 — NOT a regression: his hand-clean cut a much tighter body bbox (405px wide vs the old isolate's 577), so the BY-DESIGN clipboard silhouette (absent from the master, 19l) weighs proportionally more; eyeballed = clean aligned assembly, clipboard is the magenta delta. HALO: gone on all three (eyeballed at 3× on contrast bg — feet/leg region). REGION SPILL remains everywhere (the parts-clean layer's job, expected): Bolt's foot crop carries the far foot's dome + belt pouch in leg-upper; Checkwell's leg-upper has a hand sliver + second shoe edge; Sparkhand's leg-upper has the brown glove. Rickard judges from the edge crops which parts get parts-clean handwork. 126 app tests green; no golden diffs (current baseline bakes no worker pixels). No manifest/app-code/clipboard-generation changes.
- **2026-07-19 (task 22b, parts-clean/ hand-clean override convention + Bolt re-cut from master-clean)** — Second layer of Rickard's hand-cleaning, now auto-picked by `cut-hires.py`: LAYER 1 `hires-candidates/<persona>/master-clean.png` (19i, background/halo — replaces the threshold isolate, defringe still applied) + NEW LAYER 2 `hires-candidates/<persona>/parts-clean/<part-name>.png` (region spill — cut boxes include neighboring pixels: the other leg's ankle in a foot crop, torso edge in arm crops — unfixable at master level). A parts-clean file replaces the machine cut VERBATIM (no defringe/processing on top) and resolution happens BEFORE far-side derivation, so the cleaned near limb propagates to the far duplicate + the far-arm prop-strip automatically; `parts-clean/clipboard.png` would also outrank the generated prop. NEVER-RESIZE RULE: override canvas must EXACTLY equal the machine region size (pivots/attach-offsets are part-local fractions — a resized canvas silently corrupts assembly) → dimension mismatch or missing alpha FAILS LOUDLY with both sizes in the message; the pipeline never rescales. VISIBILITY: every run prints overrides + writes `parts-hires/<persona>/cut-log.json` (master source + overridden parts); `bindpose-diff.py` stamps that provenance on the panel footer. BOLT RE-CUT: his master-clean.png landed → auto-picked, re-cut, re-bundled to app assets, bind-pose IoU 0.942→0.950 (viewed: clean aligned assembly). FLAG: master-clean is 408×612 (cut 253×554) vs the old machine cut's 651×1402 — ~2.5× lower res; at worker render size (~80 logical px) still ~2× oversampled so it reads fine, but if a big-robot close-up lands later, re-export master-clean at full res and re-run. Checkwell/Sparkhand outputs byte-identical (no hand-cleans yet, same auto-pickup awaits their files). TESTS: sandboxed `test-cut-overrides.py` (7 checks: verbatim pickup, far leg/arm derive from override incl. through strip_prop, mismatch + no-alpha fail loudly, cut-log records); 122 app tests green, analyze untouched. Docs: new `assets-pipeline/README.md` (two-layer convention + never-resize rule). No manifest regions/pivots, app code, or output-path changes.
- **2026-07-19 (task 20c, idle wander: perspective floor plane + points of interest)** — A bored worker now drifts around its room instead of standing still. PERSPECTIVE PLANE: a worker gained a `_depth` (0 front .. 1 back); its feet ride up the band (`kDepthRiseFrac` 0.30) and its scale interpolates front 1.0 → back `backScale` (default 0.78, tunable) via the pure `depthScale()` — so moving in depth reads as walking back, not floating. The per-room camera factor (20b-fix-2) is the baseline this multiplies. POINTS OF INTEREST: registry DATA per room (`kRoomPois`) — Workshop: notice board (back center-left) + a rear desk; Archive: shelf wall + card catalog; Advisor: none. Each POI = {x, depth, activity}; activities reuse the idle animation (head-up idle, no new authoring). WANDER: a pure state machine (`wander.dart`) — rest a lazy random beat (≈20–60s) → walk to a random in-room POI → dwell (≈4s) → walk home → repeat. It NEVER runs while assigned/working/transiting and ABANDONS instantly when a job arrives (canWander=false → rest, no target); POIs stay in-room. `SkeletalWorker` runs it for AUTO-idle workers (and the STATE forcer's new WANDER, which drives it continuously for tuning). TUNING: back-scale, wander-interval, dwell sliders in the SKELETAL panel. TESTS: 122 app tests (+7: wander never preempts a job across every phase, the rest→POI→dwell→home→rest cycle, POI index stays in range, depth-scale interpolation, POI registry in-room; default-assertions cover backScale/wanderEverySec/dwellSec). analyze clean; only the debug panel golden changed (WANDER chip; new sliders below the fold — scratch-captured + viewed, clean); shipping goldens byte-identical; flag false. Live wander visuals are device-verified (a live Flame game can't be golden-tested). No shipping UI / pairing / animation-definition changes.
- **2026-07-19 (task 21, standalone RELEASE device build — the app lives untethered)** — Discovery: `device-run.sh` builds DEBUG-only, and iOS 14+ refuses to launch a debug Flutter app from the home screen — so the app had NEVER run standalone, making tethered-only dogfooding (P1 multi-hour wall-clock testing) impossible. TOOLING: added `app/tool/device-install.sh` — stamped RELEASE build (`flutter run --release` with the same `WERKZ_GIT_SHA`/`WERKZ_BUILD_NO` defines; `flutter install` can't carry `--dart-define`, so run+detach is the reliable stamped path); press `q` to detach → the release app stays on the phone and launches from the home screen, no cable. `device-run.sh` (debug hot-reload) is UNCHANGED. DEBUG TOOLS SURVIVE RELEASE: the LAYOUT TUNING panel (long-press Settings title), WORKERS toggle, STATE forcer, sliders, and the panel's persistence were gated on `kDebugMode` (false in release → they'd vanish). Re-gated on a new `kWerkzDebugTools` flag (`src/debug_tools.dart`, shipped ENABLED); `debugWorkerSprites` const semantics unchanged. Beta-hardening (hide them before beta) noted in the backlog above. Free (personal-team) signing expires after 7 days — re-run `device-install.sh` to recertify (documented in the script header + CLAUDE.md). Tooling-only: 115 app tests unchanged, analyze clean, goldens byte-identical (both flags are true in debug/test so the re-gate is a no-op there), flag stays false. No app logic / pairing / shipping UI / reconnect behavior touched. Smoke-check list for Rickard's device pass is in the task report (daemon-DOWN from the home screen: expect the unreachable banner + reachable Settings/pairing, no user-visible exception storm — if reconnect is ugly there, that's evidence for the reconnect-resilience task, not fixed here).
- **2026-07-19 (task 20b-fix-4, Advisor calibration + working `werkz` dev command)** — (A) ADVISOR CALIBRATION (Rickard's device numbers, nothing else): advisor `workerScaleFactor` 2.4→1.9. New per-room `walkSpeedFactor` on RoomDef (default 1.0), advisor 1.15 — a LENS on the single `walkSpeedPx` source (not a new speed constant; unified-speed rule intact), so the close-up room's feet read naturally. It multiplies walkSpeedPx for in-room walking and, per transit LEG, for the edge-walk: the exit leg takes the from-room's lens, the enter leg the to-room's, so the change lands in the off-view beat → no speed pop at the edge (the scale factor blends the same way, by TransitFrame.progress). Both factors are live-tunable — a scale slider + a speed× slider per floor in the SKELETAL panel. `SkeletalWorker` gained `roomWalkSpeedFactor`; `transitFrame/transitTotal/transitPhaseLabel` gained an optional per-leg enter speed (single-speed callers unchanged). (B) `werkz` DEV COMMAND — ROOT CAUSE: `bin` pointed at `src/cli.ts` whose shebang (`#!/usr/bin/env node`) is fine and Node 26 runs .ts natively (so `node src/cli.ts` always worked), but the file lacked the EXECUTABLE bit — so the `npm link` symlink couldn't be run by the OS. Fix: `chmod +x src/cli.ts` (mode committed 100644→100755); `npm link` once in `daemon/` and now `werkz` / `werkz qr` run from any project dir off this checkout (verified: `cd /tmp && werkz qr` → graceful "no running daemon" exit 1). No shim, no build step, no new deps. Dev-start note added to CLAUDE.md; `npm-placeholder/` + published `0.0.1` untouched (real `0.1.0` dist stays P3). TESTS: 115 app tests (+1: per-room walk-speed lens lookup + per-leg transit blend; scale-lookup updated 2.4→1.9; default-assertions cover 1.9/1.15). analyze clean; goldens byte-identical (new sliders below the panel golden's fold — captured to scratch + viewed, clean); flag false. No other rooms' factors, global walk speed, shipping UI, npm-placeholder, or P3 touched. Per CLAUDE.md rule 8, these are Rickard's tuned values.
- **2026-07-19 (task 20b-fix-2, per-room worker scale + unified walk speed — device tuning)** — PER-ROOM SCALE: rooms are drawn at different camera distances, so `RoomDef.workerScaleFactor` (registry data) multiplies the global height per room — Advisor close-up ×2.4, Workshop/Archive ×1.0 (wide-shot baseline). A worker's rendered height = global `workerHeightPx` × room factor × persona height-scale, applied on render via a new `SkeletalWorker.roomScale`. Factors are LIVE-tunable (`roomScaleProvider` + a slider per floor in the SKELETAL panel) so Rickard calibrates each storey's distance on device. Cross-room transit BLENDS the factor (`lerp(fromFactor, toFactor, TransitFrame.progress)`) so a worker crossing floors of different scale doesn't pop. Global height default 80→50 (the wide-shot baseline; Advisor's 2.4 puts it at ~120). UNIFIED WALK SPEED: the transit edge-walk was a separate time constant (exitSec/enterSec) — a per-state speed. Removed them; the exit/enter durations are now DERIVED from the one `walkSpeedPx` (over a reference band width) so every walking state — job walk, patrol, return-home, transit edge-walk — reads the same source and the feet never outpace the walk. Only the beat stays an independent time. Defaults: walkHz 0.5, walkSpeedPx 5→13 (so a full-room edge-walk is ~20–30 s at 13 px/s — intentionally slow, slider-tunable). TESTS: 114 app tests (+4: per-room scale lookup, cross-room scale-blend has no jump, transit edge-walk scales inversely with walkSpeedPx = one speed source, progress 0→1; default-assertion + phasing tests updated to the derived durations). analyze clean; only the debug panel golden changed (room-scale sliders) — every shipping golden byte-identical; flag stays false. No shipping-UI / pairing / animation-definition changes (only speed SOURCING). 20c stays HELD.
- **2026-07-19 (task 20b-fix, vanishing workers + unlit advisor + speed defaults — device bugs before 20c)** — ROOT CAUSE of the vanish (the actual mechanism, not just the fix): the worker model set `currentRoom` straight from `roomForEvent(e)` = `payload['room']`, which spans the FULL event-model taxonomy (§2.2) — including `test-workshop` (ANY test run: flutter/npm/pytest…) and `octagon`. The interim building only draws THREE storeys (advisor/workshop/archive), so a worker routed to `test-workshop` rendered in ZERO WorkerLayers and vanished. On device: Checkwell (round-robin cursor 0) took a dispatch whose job ran tests → `test-workshop` → gone; the main session ran a test → Bolt (primary) → `test-workshop` → gone; a later dispatch's return-home snapped them back. A no-silent-failure violation. FIX (two layers): (1) IMPOSSIBLE — `renderableRoom()` in the registry folds any unbuilt room to the workshop floor (test-workshop IS the workshop's test bench, §2.2); applied at every model room-write, so a worker is NEVER placed in a storey nothing draws. (2) NEVER-NEEDED SAFETY NET — a transit watchdog: any worker rendered nowhere (unmounted room, or a beat that outstays the longest-legit-beat+margin ≈ clock stall / orphaned chain) for >2s logs LOUDLY and snaps to the model's (renderable) room. ADVISOR: gave advisors-office explicit floor-band geometry in the registry (it was on defaults) + a test proving a worker sent there renders in the advisor layer; the highlight already uses the shared `roomForEvent` (payload['room']) so advisor-bound events light it when the daemon tags them. SPEED: walkSpeedPx 16→5 (Rickard's ~⅓ call), walkHz 0.8→0.5 (cadence sanity-passed down so the slower feet don't moonwalk), transit exit/enter 0.5→1.4 + beat 0.6→1.0 (so the edge-walk doesn't outrun the slow in-room walk) — all still slider-tunable. DEBUG TABLE: the worker readout gained a render-state row per worker (`↳ @room` / `↳ OFF-VIEW beat 0.4s` / `↳ NOWHERE:test-workshop`) — the tool for catching this bug class on device. TESTS: 110 app tests (+9: unrendered-room folds to floor ×3, watchdog recovers a forced orphan, advisor band renders a worker, property-style 400-step random room-changes+advances never orphan, + default-assertion updates). analyze clean; only the debug panel golden changed (render rows) — every shipping golden byte-identical; flag stays false. No pairing/daemon/shipping-UI touched. 20c stays HELD until this passes on device.
- **2026-07-19 (docs + bookkeeping; task 20c HELD)** — Two "do now" copy changes: (a) onboarding pre-pairing card gained a credentials line — "Werkz runs in your existing project environment — if claude can push or deploy from your terminal, your workers can too. Werkz never stores your credentials." (onboarding golden regenerated + viewed); (b) landing FAQ gained the matching Q&A ("Do I need to connect GitHub/Supabase to Werkz? No — your local Claude Code uses your existing setup; keys never leave your machine.") — landing rebuilt (astro build clean), FAQ verified in dist/index.html. Bookkeeping: idea backlog seeded (above) — Advisor Consultation + Form 22-C flagged HIGH-PRIORITY for the first post-P2.5 sprint (the consult→dispatch→file closed loop); ladder climb + big-robot close-ups parked. **Task 20c (idle wander: perspective floor plane + POIs) is HELD** — Rickard marked it "utkast, körs efter godkänt 20b-varv" and it builds directly on 20b's transit/locomotion, which is not device-validated yet. Queued as the next code increment once 20b passes on device. No app logic changed this session (docs + one shipping-copy line only); flag stays false.
- **2026-07-19 (task 20b, visual inter-room transit + room registry + dispatcher choreography + toast move)** — 20a's teleport becomes a walk. ROOM REGISTRY (`world/room_registry.dart`, rooms as DATA): each room declares id, canon vertical build order (advisor 0 / workshop 1 / archive 2), floor-band geometry (bandBottom/bandHeight — the existing mount constants, moved here), and left/right edge x. WorkerLayer + transit read ONLY the registry, so the Octagon/test-workshop later is an entry + art, zero new transit code. `roomDistance` scales the beat. TRANSIT (`world/transit.dart`, pure + time-driven): a worker walks to the NEAREST side edge (facing it) → steps past it (clipped by bounds, no pop) → an off-view BEAT (tunable ~0.6s, ×room distance) → enters the target from the corresponding edge facing inward → hands back to in-room locomotion to walk to its job/idle x. VISUAL CONTROLLER (`state/transit_provider.dart`): the model moves currentRoom instantly (20a); this turns it into the walk. A worker mid-transit is never teleported or skipped — a fresh model room-change is applied ON ARRIVAL (chained); a job that finishes while its worker is still travelling logs LOUDLY (valid: arrive, brief beat, home). Per-worker + concurrent (two can cross at once, may pass — fine v1). One `_TransitClock` ticker advances all transits; `SkeletalWorker.setTransitOverride` lets it drive x+facing while crossing. DISPATCHER CHOREOGRAPHY (20b addendum): each persona has a `homeRoom` (Checkwell = archive, Bolt/Sparkhand = floor); after a job winds down over coffee away from home, the worker RETURNS HOME (walk) so nobody stands idle covering another's spot — so Checkwell hands off on the floor then heads back to the archive. TUNING: transit beat + edge margin sliders in SKELETAL; the STATE forcer gained PATROL — the worker transits penthouse↔basement forever (Rickard's tuning loop for this feature). TOAST MOVED (20b addendum, shipping UI): the work-order toast was covering the Archive floor band → moved to the top edge, just below the status bar / above the Advisor room, still an overlay (no room-stack reflow); multi-device goldens (12/SE/ProMax) eyeballed — clears the WERKZ header + status chip, no floor covered, style unchanged. TESTS: 104 app tests (+17: registry lookup + distance, nearest-edge + exit/entry facing, transit-frame phasing + beat scaling, transit start-on-move, advance-to-arrival, room-change-applied-on-arrival, concurrent transits, PATROL loop, return-home-after-job). analyze clean; goldens changed = the debug panel + the intended toast move (+2 device variants) — every other shipping golden byte-identical; flag stays false. No ladder (parked), no dispatch-cap or other shipping-UI change.
- **2026-07-18 (task 20a, worker job-assignment + room-membership model — foundation for inter-room movement)** — The model layer under the vision "workers assigned per job, located in and moving between rooms." Visual transit is 20b (gated on this landing clean). MODEL (`world/worker_model.dart`, pure + fully unit-tested): `WorkerModelState` = list of `WorkerAgent{personaId, currentRoom, status(idle|walking|working|coffee), jobId}` + a FIFO job queue + a round-robin cursor. Single source of truth = `workerModelProvider`; the UI derives, holds no logic. A pure reducer `ingestWorkerEvent(state, event) → (state, warning?)` folds the feed: (a) ASSIGNMENT — a job-start (`worker.dispatched`/`job.assigned`) is taken by the next AVAILABLE worker round-robin; if none are free the jobId QUEUES FIFO and the next worker to free pulls it off. Not hardcoded Bolt — whoever's idle. (b) ROUTING — activity routes by jobId to that job's worker (updating its currentRoom); un-jobbed interactive activity routes to the primary worker (Bolt, event-model §1.1); building-level events (no room) are no-ops. (c) NO SILENT MISROUTES — a job event with no resolvable jobId/worker warns LOUDLY (debugPrint), never guesses. ROOM MAPPING — `roomForEvent(e)` = `payload['room']` (the daemon already computed it per event-model §2/§2.2); `home_screen._activeRoom` was refactored to call the SAME function, so highlight + model can't drift (regression-locked in a test). RENDERING — `WorkerLayer` is now room-scoped: each storey mounts `WorkerLayer(room:)`, which renders exactly the workers whose currentRoom matches (add/remove as they move → a TELEPORT between rooms, since transit is 20b). Roster distributes at start: Bolt + Sparkhand on the floor, Checkwell in the archive. DEBUG — the LAYOUT TUNING panel gained a MODEL readout (per worker: id · room · status · job, + the queue) so Rickard can watch assignment tick before any visuals. 87 app tests (+11: round-robin, FIFO queue + take-when-freed, jobId routing, primary fallback, loud-warn on unroutable, room membership + teleport, roomForEvent==highlight lock, flag-on per-room wiring), analyze clean; only the debug `home_skel_forcer` golden changed (new MODEL rows) — every shipping golden byte-identical; flag stays false. NO dispatch-cap / shipping-UI / visual-transit changes. Known 20a limitation (by design): the daemon doesn't yet emit a per-job id on every activity event, so most interactive activity resolves to the primary worker and a dispatched non-primary worker can appear/disappear between rooms without walking — 20b (transit) + a daemon per-job id close it.
- **2026-07-18 (task 19l, suite closer — corrected defaults, bundled all three personas, generated Checkwell's clipboard)** — (1) CORRECTED DEFAULTS: workerHeightPx 80 (19k had codified an earlier 99), walkSpeedPx 16 (was 22); rest unchanged; default-assertion test now locks all ten values. (2) BUNDLED Checkwell (WX-3C57, tall-thin inspector) + Sparkhand (WX-9B72, squat brute) through the FULL current pipeline (defringe, far-side no-flip duplicates, prop-strip on far arms, depth offset/tint) into app assets alongside Bolt; pubspec + bind-pose render test + far-side tests all parametrised over the three. BIND-POSE IoU: Bolt 0.942, Sparkhand 0.889, Checkwell 0.780 (Checkwell lower BY DESIGN — the generated clipboard adds silhouette the master lacks; body assembly aligns, eyeballed on all three diffs). (3) CHECKWELL'S CLIPBOARD — GENERATED (his idle master lacks the inspector clipboard). `clipboard.mjs` calls the gpt-image edit endpoint anchored on the clipboard cropped from the APPROVED pose sheet; attempt 1 produced three clean on-model boards (chose the "W INSPECTION" one). Isolated by flooding the manila background inward from the edges (the dark frame blocks the flood so the enclosed pale sheet survives — a plain dark-CC cut would punch out the paper), fitted to the manifest region aspect, folded into cut-hires as a reproducible step (`chosen.png` → `clipboard.png`). It's a distinct near-arm part, so it is NEVER duplicated onto the prop-less far arm. Clipboard outcome: SUCCESS on attempt 1, no retries needed. (4) MOUNT: `WorkerLayer` now hosts all three on the floor band via a per-persona config — Bolt (primary, event-driven, uses the workerX slider), Checkwell + Sparkhand ambient-idle at spread posts (x 0.20 / 0.82). The tuning sliders + STATE forcer apply to all three; only Bolt gets the event feed (dispatch routing to a specific worker is a later task). (5) PER-PERSONA HEIGHT — EXPOSED not silent: at the shared 80px, tall Checkwell and squat Sparkhand would read equal-height, so `SkeletalWorker.heightScale` (Checkwell 1.18, Bolt 1.0, Sparkhand 0.90) + `homeXFrac` are explicit per-persona offsets; roster preview (`parts-hires/_roster-preview.png`) eyeballed: tall inspector > round Bolt > squat brute, clipboard reads. VERIFY: three bind-pose diffs (viewed), occlusion bob-sweep + no-flip leg byte-identity locked per persona, 76 app tests, analyze clean; only the debug `home_skel_forcer` golden changed (new default slider values) — every shipping golden byte-identical; flag stays false. Halo/master-clean re-cut still pending Rickard's files (now all three, same per-persona auto-pickup). No dispatch-routing or shipping-UI changes; Bolt's tuned params untouched.
- **2026-07-18 (task 19k, far-side duplicate fixes + codified tuning defaults)** — Three far-side bugs from the walk screenshots, plus depth-tint and Rickard's tuned values. (1) FAR LEG WAS FLIPPED — the pipeline horizontally-mirrored the far limbs (`transpose(FLIP_LEFT_RIGHT)`), so in profile the far foot pointed the wrong way. Far side is now a plain DUPLICATE (`crop.copy()`, no flip); renamed "mirror"→"duplicate" through the cut script, manifest note, and worker (the root FACING mirror is separate and untouched). Locked by a byte-identity test: `leg-*-far.png` must equal `leg-*.png` (a flip or baked tint would change the bytes). (2) FAR ARM BESIDE THE TORSO — added a tunable depth ATTACH OFFSET (`farDepthOffset`, kFarDepthX/Y): the far shoulder/hip sit slightly behind (+x) and lower (+y). New occlusion test builds the real assembly and sweeps 40 bob ticks asserting the far arm's horizontal span stays inside the torso's the whole cycle (not just frame zero). (3) PROP DUPLICATED ONTO THE FAR ARM — the near arm's tool pouch (the warm leather blob in the lower-left of the forearm crop) was copying to the far arm. `strip_prop()` clears saturated-warm pixels in that quadrant on the arm-far variants only; the far arm is now prop-less (viewed). (4) DEPTH TINT moved to RENDER time (was baked `Brightness(0.72)`): far components get a 20% black `ColorFilter` (`kFarDepthTint`, tunable), so the PNG stays a plain copy and reads as behind when it peeks during a swing. (5) CODIFIED Rickard's on-device tuned SkelParams defaults: workerHeightPx 99, workerX 0.50, walkHz 0.8, walkSpeedPx 22, legSwing 0.36, armSwing 0.41, bob 3, headBob 0.06, typeHz 1.7, typeSwing 0.42 (sliders still ride on top). VERIFY: bind-pose diff IoU 0.936→0.942 (viewed, clean Bolt); 70 app tests (+3 far-side: no-flip byte-identity, depth-offset sign, bob-sweep occlusion; +2 default assertions updated), analyze clean; only the debug `home_skel_forcer` golden changed (new slider values) — every shipping golden byte-identical; flag stays false. Halo/master-clean re-cut still pending Rickard's file. Other personas untouched.
- **2026-07-18 (task 19j, debug STATE forcer — unblocks the tuning pass)** — Real dispatches finish in seconds, so walk especially never plays long enough to tune against. Added a STATE selector at the top of the SKELETAL tuning section: AUTO (default, follows real events as before) | IDLE | WALK | TYPE | COFFEE. WALK = walk-loop: patrols desk↔coffee forever, flipping facing at each end (turns via the same root mirror as 19i). The other forced states loop their anim in place (no walk-to-target). A forced state overrides incoming events until set back to AUTO (`SkeletalWorker.onEvent` early-returns while forced; `update()` branches on `forced`). Wired through `forcedSkelStateProvider` (Notifier, defaults auto) → mount → worker, exactly like the params path. Debug panel only, no shipping-UI change, no effect when WORKERS is off. 1 unit test (forced ignores events, AUTO resumes) + a scrolled panel golden `home_skel_forcer` (viewed: chips wrap inline, selection highlights green, no overflow). 67 app tests, analyze clean, flag stays false.
- **2026-07-18 (task 19i, walk locomotion + mirror-arm fix + joint simplification + speed defaults — device punch list)** — Five device issues from Rickard's walk screenshots, plus a halo fix. (1) EXTRA ARM — the mirrored far-side limbs rendered BESIDE the near arm because they were children of the near torso, and in Flame a child ALWAYS paints on top of its parent (priority only orders siblings), so a far limb could never be occluded by the torso. Fix: far limbs now live in a `_farGroup` sibling of the torso at priority root.z−10, so the whole far side paints BEHIND the torso; at rest the far arm sits fully inside the torso silhouette (its region is within the torso's horizontal extent) and is hidden. The group tracks the torso's bob so near+far move together. This ALSO resolved the "swing reads mirrored" complaint — that was the far arm scissoring in front, not a phase bug. (2) SWING SIGN — arms kept contralateral (near arm counter-phases the near leg, unit-locked); the real facing cue was the asymmetric knee-bend, removed by (3). (3) JOINT SIMPLIFICATION (Rickard's v1 call) — elbows and knees LOCKED at 0 in all v1 anims; motion is shoulder + hip + bob only. Joints stay in the rig (held at `_elbowLock`/`_kneeLock`, not deleted) so a close-up can re-enable them. work-typing now taps from the shoulder, coffee raises the arm from the shoulder. (4) LOCOMOTION — the worker owns its x on the floor band and walks toward the state's target (`targetXFor`: work station left 0.32w, coffee right 0.72w), so a dispatch reads walk-to-desk → type → walk-to-coffee → sip; a whole-sprite horizontal mirror (`facingScaleX`, same mechanism as the far limbs) flips facing when travelling right; `stepToward` snaps on arrival so the planted foot never slides past; speed is the new tunable `walkSpeedPx`. The mount stopped pinning position every frame — it just hands the worker the viewport (`setViewport`). (5) SPEED DEFAULTS halved (walkHz 1.6→0.8, typeHz 3.0→1.5, ambient idle/coffee bob rates halved) — all read too fast on device; Rickard fine-tunes via the new `walk speed` slider + existing ones. HALO: `cut-hires.py` gained `defringe()` (morphological close to fill pinholes, then erode 1px to pull the matte INSIDE the bright fringe ring, + edge decontamination) — it previously DILATED the matte (MaxFilter 5), which GREW it over background pixels and MADE the halo. Bolt re-cut from attempt1-1 with the new defringe and re-bundled; master-clean.png (Rickard's hand-clean) not present yet, so `load_master()` prefers it when it lands — RE-CUT FROM master-clean PENDING his file. VERIFY: bind-pose diff re-run (reusable `bindpose-diff.py`) — IoU 0.846→0.936 (far side no longer inflates the silhouette), viewed: clean single Bolt, no extra arm. 65 app tests (+7: facing flip, locomotion arrival/target map, contralateral swing, locked joints, halved defaults; work-typing test moved to the shoulder), analyze clean, every flag-off golden byte-identical. Other personas (3c57/9b72) left untouched. Flag stays false.
- **2026-07-18 (task 19h, fix part assembly — bind pose must reproduce the master, root-caused)** — Device (19g) showed Bolt planted + bobbing but MISASSEMBLED: arms/torso overlapping wrong, far leg reading detached, belt mid-body. Built the verification FIRST, then fixed against it. ROOT CAUSE (one bug, in the RENDERER, not the manifest/cut/regions): `build()` positioned each child with `pivotWorld - parentPivotWorld`, but Flame positions a child from the parent's TOP-LEFT, not its pivot — so every part below the root was off by the parent's pivot-within-region offset, and those errors compounded down the chain (torso→arm-upper→arm-lower drifted furthest, far-side limbs worst because the mirror chain added another offset). Fix: extracted a pure `bindPoseXform(part, parentTopLeft, renderW, renderH)` that returns `localPos = pivotWorld − parentTopLeft` AND the part's own `worldTopLeft` to thread to its children; `build()` now threads world top-left down the near tree and the far chain instead of pivot. No manifest region/pivot/attach edits were needed — the cuts were right; the assembly math was wrong. Regions/pivots untouched, so 19e's flagged approximation debt was NOT the cause (bind-pose diff shows near-side silhouette lands on the master; far-side magenta bleed is just the intentional 5% depth nudge, not misassembly). VERIFICATION (shares the real assembly math): `bindpose_render_test.dart` renders the actual SkeletalWorker in bind pose (all joints 0) at master resolution via renderTree→toImage and `cut-hires` composites MASTER | RENDER | OVERLAY → `parts-hires/7a19/bindpose-diff.png` — render now reproduces Bolt as a robot, not a parts pile. REGRESSION LOCK: `bindpose_math_test.dart` walks a synthetic off-centre-pivot 3-part manifest through `bindPoseXform` and asserts every part's `worldTopLeft` == its region top-left and the composed anchor == its pivot-world — so the pivot-vs-top-left bug can't return silently. WORKERS toggle: `debugWorkerSpritesProvider` is now a `NotifierProvider<bool>` defaulting to the const `debugWorkerSprites` (still the master gate + default, false in repo); the LAYOUT TUNING panel's new WORKERS switch flips it in-app, so the const never gets hand-edited for a device test again. Flag stays false; every flag-off golden byte-identical; 58 app tests (+1 assembly lock), analyze clean. Render artifact `_bindpose-render.png` gitignored; `bindpose-diff.png` kept as evidence.

- **2026-07-18 (task 19g, fix mount scale + position — root-caused, not tweaked)** — Device showed Bolt oversized + anchored off the left edge. Found the actual divergence before touching constants. ROOT CAUSE 1 (position, real code bug): `_WorkerGame.onGameResize` set the worker's anchor/position, but Flame fires onGameResize BEFORE onLoad creates the worker (worker null → skipped) and doesn't re-fire — so the worker stayed at Flame's default topLeft/(0,0), i.e. the left edge, never bottom-anchored to the floor. Fixed: position is set after add() in onLoad, on every resize, AND every frame (so the workerX slider is live); anchor bottomCenter now lives in SkeletalWorker.onLoad. ROOT CAUSE 2 (scale, NOT a code bug): measured the actual math — the mount computes the correct ~130 LOGICAL px (masterPxH 459 → scale 0.283 → renderH 130), which is ~390 physical on @3x; the "3–4x / 400px" was a logical↔physical read plus 130px being simply too big for a background worker. Made size explicit + tunable: `SkelParams.workerHeightPx` (default lowered to 110, "background" scale) live-scales the whole worker from the feet, and `workerX` moves it — both in the SKELETAL tuning section, so Rickard dials the design scale (and his "big robot for special moments" idea) live. The scale math is extracted to a pure `rigRenderSize` and unit-tested (height == target). Preview gap closed: `mount-preview.png` regenerated with the SAME scale (whole-worker → workerHeightPx) + position (bottom-center) as the mount — but honestly marked as size/identity verification only, since a Python composite can't exercise Flame's lifecycle (that's what hid the position bug; device is authoritative). Flag reset to false (was flipped locally for the test); every flag-off golden still byte-identical (skeletal sliders sit below the panel fold). 56 app tests (+1 scale), analyze clean.
- **2026-07-18 (task 19f, mount SkeletalWorker behind the debug flag)** — Wired the 19e component so the device demo can run. `WorkerLayer` (`world/worker_layer.dart`) hosts one Bolt SkeletalWorker in a transparent Flame `GameWidget`, mounted on the WORKSHOP FLOOR band by `StackedWorkshop` ONLY when `debugWorkerSpritesProvider` is true (defaults to the const `debugWorkerSprites`, still false in the repo — a provider so a test can override without a flag flip). Feet bottom-anchored to the floor, rendered at ~130px (the 100–140 target). Live wiring: `ref.listen(workshopProvider)` pushes the newest feed event → `worker.onEvent` (the pure mapping: dispatched→walk, task.started→work-typing, done→coffee-idle); the SKELETAL tuning sliders push into `worker.params` each build → real-time. Loud failure: a missing manifest/part throws in the game's onLoad and `GameWidget.errorBuilder` paints a visible red "WORKER MOUNT FAILED" — never a silently empty room. HONEST on the golden: a live Flame GameWidget CANNOT be captured in flutter_test goldens (its continuous ticker + the HomeScreen 500ms timer trip the pending-timer invariant, and async image decode won't finish in one captured frame) — so instead a faithful mount-preview (Bolt reassembled from the SAME bundled parts at the SAME manifest positions, on the real workshop-floor art at target size) is saved to `parts-hires/7a19/mount-preview.png` and viewed (on the band, no clipping, right size). Zero-footprint-when-off is proven two ways: every existing golden re-verifies byte-identical with the flag off, and a test asserts `find.byType(WorkerLayer)` is nothing at the default. Flag stays false; no shipping-UI change. 55 app tests (+1 gate), analyze clean.
- **2026-07-18 (task 19e, DIRECTION CHANGE — Rive dropped, Flame-native skeletal player)** — Rive moved .riv export behind a paywall (Cadet $9–17/mo, Oct 2025) — the free editor can't ship files. Decision (Rickard): drop Rive entirely; the rig manifest IS the rig, played by a programmatic Flame component with coded joint rotations (rigid motion suits the 1955 robots). Everything stays on the 19c contract so Rive can be revisited post-beta. Built: (1) CUTTING — `cut-hires.py` isolates the approved attempt1-1 hi-res masters, cuts parts per each rig-manifest to `sprites/parts-hires/<persona>/`, generates mirrored far-side limbs, and a debug-overlay.png; fractions land APPROXIMATELY on the hi-res masters (arms differ slightly, 3C57 has no clipboard) — flagged, no manifest edits, contract intact. Occlusion inpaint is minimal (no numpy) but the idle near-arm hangs beside the body so bleed is small (reassembly crop shown). (2) PLAYER — `rig_manifest.dart` (loud-fail validation: missing part/pivot/dangling parent throws), `worker_animations.dart` (the five contract animations as coded joint angles + bob, tunable `SkelParams`), `skeletal_worker.dart` (Flame `SkeletalWorker` builds the near+far joint tree, loads sprites, animates). worker_sprite.dart's event→state mapping + the 19c `WorkerRig` contract kept as the code API; `inputsForState` unchanged (mood still the number 0/1/2). (3) Bundled Bolt (WX-7A19) parts (~293KB) + manifest in app/assets (Bolt first); flame_rive + rive REMOVED from pubspec. (4) LAYOUT TUNING panel gained a SKELETAL section (walk hz, arm/leg swing, bob, head bob, type hz/swing → skelParamsProvider) for the same live-tune-then-codify loop. Behind `debugWorkerSprites` (false) — NO shipping-UI change. 54 app tests (+8: manifest loud-fail, animation motion, contract lock, skeletal state), analyze clean, flame_rive removed cleanly, tuning golden re-viewed. Device demo + honest limits in `.claude/handoff/task-19-skeletal.md` (renamed from task-19-rive.md).
- **2026-07-18 (task 19d, regenerate hi-res masters — candidates ready for the gate)** — 19b's wall (approved masters 237–266px, upscale from day one) cleared by regenerating dedicated hi-res masters via the gpt-image EDIT endpoint, anchored on the approved isolated master-idle.png so the profile angle + pose stay valid (edit = reference + prompt, not text-to-image). HONEST endpoint note: Fal has no gpt-image-2 edit endpoint (verified 404) — edit exists only on `fal-ai/gpt-image-1/edit-image`, so that's what was used (gpt-image-2 is text-to-image only on Fal). Result: all three personas nailed it on ATTEMPT 1 (of 4 allowed) — clean TRUE LEFT-SIDE PROFILES, single robot on plain paper, character heights 1410 / 1536 / 1430 px (7A19 / 3C57 / 9B72), all far past the ≥900px target and the 840px 2× headroom. Silhouette test vs the approved silhouettes + matched-scale comparison images confirm each reads as the SAME character (round hard-hat generalist / tall-thin visored inspector / squat goggled brute). Candidates (2 each) + `comparison.png` + `silhouette-compare.png` per persona in `assets-pipeline/sprites/hires-candidates/<persona>/`; recommended pick is attempt1-1 for all three (correct WX-IDs, closest to approved — 9B72's attempt1-2 drifted its ID to "WX-0812"). STOPPED at generation per the task: NO cutting, NO manifest changes — Rickard gates the picks, then cutting + fraction-overlay verification is task 19e. Assets-pipeline only; no app/parts/manifests/pubspec touched.
- **2026-07-18 (task 19c, fix the Rive input contract + pin the name contract)** — Gate review caught a blocker in the task-19 handoff: Rive state-machine inputs are number/boolean/trigger ONLY — there is no string/enum type, so the `mood` input specced as `routine|busy|maintenance` cannot be authored in the editor. Fixed in all three places before Rickard's session. (1) `mood` is now a NUMBER: 0=routine, 1=busy, 2=maintenance, codified as `WorkerMood` with explicit `.riveValue` in `worker_sprite.dart` — no magic numbers at call sites. `inputsForState()` now emits exactly `speed` (double 0..1), `mood` (double 0/1/2), `carrying` (bool); unit tests assert each event-state's mood number. (2) Pinned the NAME CONTRACT as `WorkerRig` constants (artboard `worker`, state machine `worker`, inputs `speed`/`mood`/`carrying`, animations `idle`/`walk`/`work-typing`/`carry-walk`/`coffee-idle`, kebab-case) + a table in the handoff; a test asserts the exact strings + kebab-case so the follow-up wiring validates against them. (3) Updated all three `rig-manifest.json` state-machine-inputs (mood string→number mapping; regions/pivots/z-order/attachParents UNTOUCHED) and the generator template. (4) Runbook rewritten: step 4 uses the number input + mapping table; new step 0 (export a trivial smoke-test .riv, load it via rive 0.14.9/flame_rive 1.11.1 on device BEFORE rigging — fail = loud runtime-version decision); a "Bolt first" section (prove the whole loop on WX-7A19 with the CC name-resolution validation before rigging the other two); safe pause points (after export, never mid mesh-binding); expectations (steps 1–4 mechanical ~half evening/persona, step 5 the real animation work, v1 = "ugly but readable"). No assets-pipeline part files, shipping UI, or pubspec touched; `debugWorkerSprites` stays false. 46 app tests (+3 contract), analyze clean.
- **2026-07-18 (task 19b, hi-res re-cut — BLOCKED by source resolution, no silent workaround)** — Goal: re-cut the rig parts at hi-res before Rickard's Rive session (task-19 masters were 237–266px; workers render at ~420px physical on iPhone 12 @3x, so they'd upscale from day one). FINDING, reported loudly rather than worked around: a hi-res re-cut is NOT possible from the approved sheets while keeping the manifest valid. The idle-stand master pose (true side profile, what the manifest fractions were built for) exists only in the pose sheet at ~1/6 of a 1024×768 sheet → 237/266/261px (all ~0.6× the 420px 1× target — UNDER 1×, not just under the 840px 2× alarm). The turnaround views ARE bigger (~487–613px) but unusable: only WX-3C57's turnaround "SIDE" is a true profile (7A19/9B72 render front/3-4 — the gpt-image-2 profile weakness), AND all are a different neutral pose whose bbox/proportions break the resolution-independent fractions (verified by overlaying them on the hi-res 3C57 master — they misalign). So there is no higher-resolution rendering of the manifest's pose to re-cut, and re-cutting from the turnarounds would need new fractions [forbidden] and the wrong angle for 2 of 3. Delivered: `debug-overlay.png` per persona (fractions verified landing on the correct-pose masters), a `_hires-report/` folder with the evidence (turnaround-side views, 3C57 fraction-misalign overlay) and `REPORT.md` with the per-persona numbers + the deliberate decision — (a) regenerate dedicated large true-side masters as task 19c [clean fix, needs go-ahead], (b) accept low-res (upscales), (c) upscale (cosmetic). Masters/parts UNCHANGED. Assets-pipeline only — no app/pubspec/UI touched. This is a genuine wall in the source art, surfaced for a call, not a quiet accept.
- **2026-07-18 (task 19, sprite pipeline prep — Rive path, up to the editor door)** — Pose gate PASSED (Rickard approved candidate-1 across all three); path chosen: RIVE rigging. CC prepared everything up to the one step it CANNOT do — `.riv` artboards are authored only in the Rive editor (rive.app); the `rive` runtime (0.14.9) and `flame_rive` (1.11.1, resolves against our flame 1.37) only play + control them. (1) CUTTING: isolated each persona's idle-stand master pose (largest-connected-component background removal, soft masks) to `assets-pipeline/sprites/parts/<persona>/master-idle.png`, plus best-effort part crops (head/torso/arms/legs + prop). HONEST: flat cuts leave occlusion holes (torso behind an arm isn't in the pixels) and far-side limbs are editor mirror-duplicates — the crops are a scaffold, not finished layers. (2) RIG MANIFEST per persona (`rig-manifest.json`): every part's master region, pivot (joint), z-order, attach parent, plus the v1 animation targets (idle / walk / work-typing / carry-walk / coffee-idle) and the state-machine inputs (speed 0..1, mood routine|busy|maintenance, carrying bool) — this is what makes the editor session mechanical rather than creative. (3) FLAME PREP: `app/lib/src/world/worker_sprite.dart` — the WerkzEvent→state mapping (dispatched→walk, task.started→work, done→maintenance, event-model §2) with a unit test, and documented Rive wiring; `flame_rive` added to pubspec (eval done). Nothing mounts into shipping UI (`debugWorkerSprites=false`, no visual change). THE HONEST BOUNDARY: rig assembly + occlusion cleanup + animation polish in the Rive editor is Rickard's hands-on session (est. 1–2 evenings; the parts + manifests make it mechanical). Steps in `.claude/handoff/task-19-rive.md`; after he exports `<persona>.riv`, a short CC follow-up wires `_apply()` + mounts sprites on the floor band. 43 app tests (+3 worker-sprite mapping) · analyze clean.
- **2026-07-18 (task 18 B, worker sprite pose sheets — P2 slice 2 opener)** — Generated the pose reference for the three canon robot workers via gpt-image-2 (Fal), silhouettes locked to the personnel file: WX-7A19 round generalist, WX-3C57 tall-thin inspector, WX-9B72 squat brute. Per persona a TURNAROUND sheet (side/3-4/front) and a side-view POSE sheet of six labelled poses (idle-stand, walk-contact, walk-passing, work-typing, work-carry, maintenance-coffee), 2 candidates each, style-anchor prefix, painterly game-art in the room palette. Picked candidate 1 across the board for cross-pose consistency (the same robot in all six — gpt-image-2's known weak spot) and label fidelity. SILHOUETTE TEST passed: thresholded each winning pose sheet to pure black (`silhouette.mjs`) and viewed small — round vs tall-thin vs squat-wide read as three distinct shadows (proofs saved). Honest weakness noted: a true left PROFILE is unreliable from gpt-image-2 (the "SIDE" view reads front-ish) — fine for the pose gate, the animator works from the full set. Winners + prompts in `assets-pipeline/approved/sprites/`; anchors/README.md gained sprite-pose canon; new manifest jobs `sprite-*`. STOPS AT THE POSE GATE — no Flame integration, no cutting, no animation until Rickard approves. Next decision after approval documented in `.claude/handoff/task-18-sprites.md`: Rive rigging (rig once per body type, smooth, tiny assets) vs frame-by-frame (simpler pipeline, many more gpt-image-2 iterations, cross-frame consistency is the risk).
- **2026-07-18 (task 18 A, layout codified as ratios — SAGA CLOSED for real)** — Rickard tuned the final spacing ON DEVICE (iPhone 12) via the debug panel and read the numbers back: app-bar gap 0, advisor 241 / workshop 220 / archive 204, advisor band-y −1.0 (top of art), workshop band-y +0.06, all cover, seams as shipped. Codified NOT as absolute pixels but as RATIO WEIGHTS: `StackedWorkshop` now distributes the available content height in the 241:220:204 proportion via LayoutBuilder, so the same look fills an SE, a 12, or a Pro Max; a per-storey `roomMinHeight` (160) floor means a small screen degrades to SCROLL, not squish. When nothing is floored the storeys fill exactly — no scroll, no gap. These are the `LayoutTuning` defaults now; the debug panel stays (height sliders relabeled "ratio", added a min-height slider) so it matches shipped values. Multi-device goldens added and viewed at 375×667 (SE), 390×844 (12), 430×932 (Pro Max) — all fill in the same proportions. The 16→18A layout arc is closed: the answer was tune-on-device + express as ratios, not remote constant-guessing. 40 app tests (+2 device goldens) · analyze clean. (Part B — worker sprite pose sheets — follows.)
- **2026-07-18 (task 17f, LAYOUT SAGA CLOSED — chips won)** — Rickard's design call: he prefers the original signage, not the 17d/17e UI nameplate slabs. (1) Reverted the nameplate rendering by restoring `stacked_workshop.dart` from git (a0ef85b) — corner floor-name chips (ADVISOR / WORKSHOP FLOOR / ARCHIVE, top-left, active storey green) and the plain thin steel slab between storeys, exactly as before; removed the now-unused vector-W brand asset + pubspec entry. (2) Cropped the advisor's baked bottom WERKZ-plate strip from the EXISTING approved asset (892×474 → 892×445, cut at row 445 where the warm wood floor gives way to the neutral steel rail) — the room above the strip is untouched and already approved, no regeneration. Advisor art now ends at the floor/carpet edge like the other two rooms, so there is no double signage against its corner chip. Advisor stays on cover (the fitHeight excursion remains gone). (3) Goldens re-viewed at 390×844: three rooms, thin uniform seams, chips back, advisor floor clean, no double signage anywhere. The whole 16→17d layout arc is now closed: the correct answer was neither a render parameter nor a UI slab but the smallest asset edit (crop 29 px) plus the design the room-art was built for. 38 app tests (nameplate test → chip test) · 101 daemon untouched · analyze clean.
- **2026-07-18 (task 17d, the plate wasn't uniformly in the art — signage moves to the UI)** — Rickard asked the question that ended the saga: does the cropped advisor image even CONTAIN the plate? VERIFIED FIRST (should have been step one four tasks ago): viewed all three shipped assets. Finding, honestly not quite what was predicted: the advisor asset DOES still carry a baked steel "⟨W⟩ WERKZ" nameplate rail at its very bottom edge (its frame was never re-cropped) — but workshop and archive have NO such rail (task 11.4 cropped theirs). So the goal (a matching nameplate on every storey) was UNACHIEVABLE from the art regardless of renderer: two of three rooms simply don't have the plate. Not missing pixels — INCONSISTENT pixels, which is worse, because no fit/align/height setting can conjure signage that isn't there. Decision (Rickard, Option A): stop chasing it in rendering. (1) Advisor reverts to cover like the others; the fitHeight excursion is removed (the per-room fit toggle stays in the debug panel — harmless). (2) Signage is now a UI-layer widget: a 1955 brass/steel NAMEPLATE (the vector W tinted to read on steel + the stenciled room name, riveted ends, active storey in approval-green) heads EVERY storey — uniform, scalable, no image regeneration ever again. (3) The redundant floor-name corner chips are gone; the vector W is bundled at assets/brand/werkz-w.png. Golden: all three nameplates render (ADVISOR'S OFFICE / WORKSHOP FLOOR / ARCHIVE) at 390×844, viewed. THE ROOT-CAUSE CHAIN, written down as the lesson: task 16 the golden harness LIED (no safe-area insets) → task 17 we couldn't even prove which BUILD was on the phone (build stamp + on-device tuning) → 17b/17c the FIT MODEL was wrong (cover can't show full height; and at the iPhone-12 golden geometry cover==fitHeight, so the golden never disagreed with the tuning) → 17d the pixels weren't uniformly THERE. Every round tuned a renderer against a goal the layer below couldn't deliver. Lesson for the file: VERIFY THE ASSET CONTAINS THE GOAL BEFORE TUNING THE RENDERER. 38 app tests (+nameplate render, fit default flipped to cover) · 101 daemon untouched · analyze clean · handoff doc updated.
- **2026-07-18 (task 17c, THE root cause — cover vs fitHeight)** — Honest accounting: tasks 16, 17, and 17b all tuned parameters INSIDE `BoxFit.cover`, a rendering that geometrically cannot produce the goal. cover scales the art to fill BOTH the slot's width and height and crops the overflow — so raising a room's height just zooms in and crops MORE, and no crop-alignment value can reveal art that cover has decided to clip. The advisor's signage plate is baked BELOW its floor line, so cover could NEVER show it; every "advisor spacing" round was doomed from the model, not the numbers. Rickard verified the zoom-on-device behaviour and named it. Fix = per-room FIT MODE (`RoomFit` in LayoutTuning). The advisor now renders `BoxFit.fitHeight`: the full art height ALWAYS shows (plate guaranteed), the sides crop against the screen width, and its align slider PANS X (which horizontal slice shows) instead of picking a Y band. Workshop and archive keep `cover` because their signage is mid-art. The tuning panel gained a COVER | FIT-H toggle per room and relabels the align slider ("pan-x" vs "band-y") to match the active mode. Acceptance test that was never written now exists: `layout_tuning_test` asserts the advisor Image renders fitHeight and the others cover, and the home_ambient golden shows the desk plaque AND the floor plate in ONE render (viewed at 390×844). DEEPEST honest note — WHY the goldens never caught this across 16/17/17b: at the exact iPhone-12 golden geometry the advisor slot is narrower-aspect than the 892×474 art, so `cover` there was HEIGHT-driven and coincidentally rendered pixel-identical to `fitHeight` (home_ambient.png is byte-unchanged by this commit). The golden always looked right; the crop axis flips with the real device's effective slot aspect, so the device cropped while the golden did not. `fitHeight` is geometry-invariant — full height always, everywhere — which is the whole point. 37 app tests (+fit-mode acceptance) · 101 daemon untouched · analyze clean · handoff doc updated with the fit-mode rule.
- **2026-07-18 (task 17b, tuning-panel usability + the real advisor fix)** — Rickard's insight cut through three rounds of guessing: the ADVISOR is the only room whose signage plate is baked BELOW its floor line, so any uniform-height slot crops it away — no crop-alignment value can fix a slot that's simply too short. Fix: per-room SLOT HEIGHTS. The advisor now ships TALLER (advisorHeight 288 vs 224 for the others — also thematically right for the penthouse), so cover shows its full art height incl. the below-floor plate. The room stack became a SingleChildScrollView of fixed-height storeys, so when they total more than the viewport (they now do) it SCROLLS — a taller penthouse or a docked tuning panel can never permanently hide a seam. The LAYOUT TUNING panel is now a compact, DRAGGABLE overlay: drag the header (or the ⇅ button) to dock top/bottom, collapse to a thin bar, rooms scroll underneath (foot scroll-room added so a bottom-docked panel scrolls past). New sliders for the three slot heights; the crop-alignment sliders relabeled "(band)" and captioned so it's clear align picks WHICH band of the wider art shows while height sets the slot. Tuning values now PERSIST across restarts in debug (secure storage, key werkz.layoutTuning) so a rebuild doesn't wipe an in-progress session; RESET clears it. Handoff doc updated with the new slider→field map. 36 app tests (tuning test retargeted to the advisor-height slider) · 101 daemon tests untouched · analyze clean · home goldens + home_layout_tuning re-viewed at 390×844 (advisor plate now fully reads; panel compact with rooms visible above).
- **2026-07-17 (task 17, decision lifecycle + build stamp + LIVE tuning)** — HONEST ACCOUNTING first: task 16's spacing pass did NOT resolve on device — Rickard reported IDENTICAL spacing, plus a 6-hour-old zombie decision (requisition 41-W from 17:31) re-presenting on every app restart. Two lessons. (1) We could not even prove WHICH build he was running — both reports are consistent with an old binary on the phone, and we had no way to rule that out. That ambiguity is now dead: the app shows build number + git short-sha in Settings and tiny in the onboarding corner (UNSTAMPED if built outside `app/tool/device-run.sh`, which injects sha + commit-count via --dart-define), and the daemon prints its own sha at startup. Every future device report starts from a known build. (2) Remote-guessing spacing constants from goldens is a dead method after three failed rounds — replaced by a debug-only LAYOUT TUNING panel (long-press the Settings title in a debug build): live sliders with visible numbers for the contested constants (app-bar top gap, per-seam slab heights, per-room crop alignment, bottom bar height/pad). Rickard tunes on the phone in two minutes, reads the numbers back, CC hardcodes them into `LayoutTuning` defaults. All constants routed through that provider; defaults = shipped values, home goldens byte-identical. — DECISION LIFECYCLE root cause fixed in the daemon: pending decisions could outlive their CC sessions (socket death was only handled at daemon restart; the away ceiling is 2h of setTimeout that a sleeping laptop can stretch). Three layers now: (A1) the /pretooluse hold registers the hook socket — if it closes before we reply, the session is dead and the decision is superseded IMMEDIATELY (reason session-ended); (A2) a 60s TTL sweep supersedes any hold with a dead socket, and any hold older than pendingTtlHours=3 (no legitimate ceiling reaches that age — reason stale-ttl); (A3) listPending/welcome-snapshot only ever returns decisions with a LIVE held connection and self-heals on sight if a close event was missed. decision.superseded (and approved/denied) now carry decisionId so the app drops exactly that requisition. 101 daemon tests (+5 lifecycle, incl. e2e: real socket destroyed → snapshot empty within seconds) · 36 app tests (+2 tuning panel) · analyze clean · changed goldens (settings, onboarding, new home_layout_tuning) viewed at 390×844.
- **2026-07-17 (npm name secured)** — `werkz@0.0.1` placeholder published to npm and verified: `npx werkz@latest` prints the 1955 teaser ("WERKZ INDUSTRIES — Department of Agent Operations. The workshop opens soon. → werkz.app") and exits 0. Package lives in `npm-placeholder/` — 4 files (package.json, cli.mjs, README, MIT LICENSE), deliberately ZERO daemon source; npm 11.16 quirk found and fixed (a `./`-prefixed bin path gets the bin entry silently stripped at publish — `npm pkg fix` normalizes it). Two pre-beta tasks queued under "Now": real daemon as `werkz@0.1.0` (needs the dist build — bin is a raw .ts today) and npm trusted publishing via GitHub Actions OIDC (bypass tokens restricted Aug 2026/Jan 2027). Process rule added to CLAUDE.md after the publish flow asked for an OTP in chat: sensitive credentials never go through the conversation — Rickard runs those commands himself.
- **2026-07-17 (task 16, ROOT CAUSES — stop tuning constants)** — Two systemic defects behind the same spacing bug reported 5–6 times, plus ghost decisions. (A) THE GOLDENS LIED. The harness wrapped the app in a MediaQuery with iPhone-12 padding, but MaterialApp builds its OWN MediaQuery.fromView(tester.view) which shadows any ancestor — so every "verified" golden rendered with ZERO safe-area insets. The notch (47) and home-indicator (34) that reshape the real layout were never present, so no amount of constant-tuning could match the device. Fix: set the insets ON THE VIEW (`tester.view.padding`/`viewPadding` via FakeViewPadding), pump the app directly, regenerate every golden. Then, against the corrected renders, fixed in one pass: (1) app bar tight under the status bar; (2) advisor floor W-plate now fully visible — at the iPhone-12 layout the panels are narrower-aspect than the 892×474 art so cover crops HORIZONTALLY and the full image height shows (the crop-axis was the real variable, not the alignment constant); (3) both floor seams use one identical steel slab; (4) bottom bar height = content (42) + home-indicator inset only, killing the dead steel under the labels (was a too-tall 54 content box = the "double padding"). (B) GHOST DECISIONS + IMMORTAL BANNER: replay was treated as live. On reconnect the daemon replays history, and the app re-ran every replayed decision.requested / job.* through the live handler — resurrecting decided overlays and finished-job banners. Fix: the daemon now marks replayed WS frames `replay:true`; the app short-circuits replayed events to the log only. Pending decisions derive ONLY from the connect snapshot (onWelcome); a resolved/superseded decision can never re-present, and a restored terminal job state never re-fires the banner. Also moved the work-order banner to a Stack OVERLAY so it never reflows the room stack when it appears/dismisses. Tests: reconnect-after-decided → zero overlays; replayed job.completed → banner stays idle; live decision still presents. 33 app (+reconnect-ghost, corrected goldens) · 96 daemon (+replay flag) tests, analyze clean, all goldens re-viewed at TRUE 390×844 insets.
- **2026-07-17 (task 15b, docs only)** — Parked two future ideas, no code. (1) Assembly Line — chained agent pipeline (Advisor PRD → planner → builders → reviewer → Advisor verdict → user stamp), rendered as a 1955 pneumatic-tube factory line; reuses dispatch + decision gates + plan-mode events; explicitly gated to AFTER the Octagon (its Agent SDK orchestration is the learning ground). (2) Slice-2+ tuning bucket: raise the concurrent dispatch cap 1 → N once sprites land (parallel robots, each already has a workerId), and document in the onboarding FAQ that rooms map to activity type (GDD §4.2), not repo areas. — Device pass on the ambient screen. (A) Replaced the floating round FAB + the permanent collapsed log strip with a thin 1955 steel BOTTOM BAR: stenciled [LOG] and [DISPATCH] tabs, subtle corner rivets, a third slot reserved (commented) for the P4 Octagon. LOG opens the incident log as a modal draggable sheet (newest-on-top) — nothing permanent when closed, so the rooms now render clean to the bar's edge; a stamp-red unread dot rides LOG when new entries have landed since it was last opened. DISPATCH opens Form 17-B. (B) Stamp timing: added a device-tuned ~200ms beat between the card's fly-off and the verdict stamp slam so the card visibly leaves before the stamp hits (constant commented for hardware tuning). (C) Spacing round 2, tuned against the real 390×844 render: app bar sits tighter under the iOS status bar; the advisor room's cover alignment biased down so the desk W-plate and the floor WERKZ plate both read clear of the seam; floor slabs are now an explicit uniform steel element (lit top edge) at BOTH seams so the storeys read consistently regardless of how dark the adjoining art is. (D) Work-order banner lifecycle: COMPLETED self-dismisses after ~2.5s (tap the ✕ to clear early), FAILED stays until tapped (failures must be seen), IN PROGRESS persists while the job runs. 30 app tests (+work-order banner lifecycle, +3 home goldens: bar-closed, log-open, dispatch-sheet), flutter analyze clean, all goldens viewed at 390×844. Daemon untouched (96 tests still green). Overlay stamp-delay verified not to break the swipe tests.
- **2026-07-17 (task 14, always-on honesty + sleep handling + landing how-it-works)** — The product needs the user's machine awake and reachable; task 14 makes that COMMUNICATED, HANDLED, and PARKED. (A) Onboarding gains a plain-spoken 1955 callout: "THE WORKSHOP LIVES ON YOUR COMPUTER" — runs where your code does, not our servers; for away-from-desk decisions the machine must be on and awake; macOS holds it awake while the workshop is open; Cloud Workshop is a planned tier. Honest, not apologetic (target users run always-on machines). (B1) Daemon holds idle-sleep on macOS via `caffeinate -i -m -w <pid>` tied to its own lifetime (auto-cleans when the daemon dies), `--no-caffeinate` opts out, off-macOS is a no-op with an honest note; lid-close-still-sleeps caveat stated everywhere, never hidden. Startup prints the awake status. (B3) App no longer shows a silent dead screen when the daemon stops answering: after two failed reconnects the client flags UNREACHABLE, the status bar reads WORKSHOP UNREACHABLE and a red banner names the causes (machine asleep/off, phone on a different network, workshop stopped) + "reconnecting automatically". Pending decisions already persist and WS reconnects with backoff (built tasks 5/7); the only untestable-headless piece — a real sleep/wake cycle — is now an explicit P1 hardware gate for Rickard. (C) Landing (landing/) rebuilt from placeholder: "HOW IT WORKS" (terminal → daemon → phone, free = same wi-fi / Foreman = relay anywhere, machine must be on) + FAQ answering does-my-computer-need-to-be-on (yes, honest + caffeinate), what-happens-when-it-sleeps (unreachable + state survives + lid caveat), and can-you-host-it (Cloud Workshop, waitlist). 1955 voice throughout; static build passes. (D) Cloud Workshop parked under P4+/post-launch with pricing intuition (above Foreman — hosted compute + uptime, not just connectivity) and a note that BYOK-on-our-machine needs a key-custody/trust design before build. 96 daemon (+caffeinate) · 26 app (+unreachable state, +2 goldens home_unreachable) tests, analyze clean, all changed/new goldens viewed (onboarding callout checked via a full-height scratch render).
- **2026-07-17 (task 13, dispatch silent-failure + preflight diagnostics + UI polish)** — Device findings. (A) A work order logged "dispatched" then vanished — no job.completed, no error, no file. Root cause: the daemon is spawned from Node (npx), not a login shell, so a bare spawn('claude') misses the binary when `claude` is a shell alias or lives under ~/.claude/local. Fix: resolve an ABSOLUTE claude path once at startup (explicit config → `which claude` → known install locations, each confirmed with `--version`), spawn that, and CAPTURE stderr + exit code always. New TERMINAL-EVENT RULE — every dispatched job now emits exactly one of job.completed{ok,turns} or job.failed{reason,detail}; a missing claude fails loudly (FILED → FAILED), never a vanish. App shows live status FILED → IN PROGRESS → COMPLETED/FAILED and narrate() has templates for the new events. (B) Preflight diagnostics (Rickard's design): the daemon checks its environment at startup — claude found+version, our hook installed in this project, Node ≥22, port bindable — and ships the result on /status, /preflight, and the WS welcome. If any check fails the app raises a red actionable banner ("Workshop cannot dispatch — Claude Code not found. …werkz config claude-path"), tap for the full diagnostics sheet; the terminal prints the same checklist. New `werkz config claude-path <path>` subcommand persists an override to gitignored .werkz/config.json. No silent degradation — the pattern generalizes to every future dependency. (C) UI polish, each golden-verified at 390×844: app icon plate now follows a rounded-square that tracks the iOS corner mask (was a square plate inside the round mask) and the W is ~18% larger, re-rendered to all sizes and checked at 60×60; the notch/status-bar inset is painted steel gray so the clock/battery sit on the app's chrome, not a cream stripe; per-room cover alignment keeps each floor's baked WERKZ signage from being sliced at a divider; FILE WORK ORDER FAB is now a small round icon-only clipboard button above the drawer handle with a tooltip. Two new goldens (home_preflight_fail, home_work_order). 93 daemon (+claude-path/preflight/terminal-event) · 25 app tests, analyze clean, all goldens viewed.
- **2026-07-17 (task 12, regression sweep + screenshot discipline + app icon)** — Device verdict on the 11.5 layout work was "påse bajs" — shipping UI unverified is banned now. (A) New STANDING RULE in CLAUDE.md: every task touching app UI must generate golden screenshots at the iPhone-12 logical size (390×844) for each changed screen, and CC must VIEW every image before claiming done — a layout bug visible in a screenshot that still reaches Rickard is a process failure. Built a golden harness (`app/test/golden/harness.dart`, notch padding simulated) + 5 goldens (onboarding, first_run, home_ambient, home_decision, settings). (B) Regressions fixed, each screenshot-verified: (1) practice-swipe was dead — the onboarding PageView stole the horizontal drag; set it NeverScrollable and added `practice_swipe_test.dart` that actually drags the card to a committed APPROVED. (2) Onboarding swipe-hint row overflowed 54px (the golden caught the exact overflow) → FittedBox scaleDown per hint. (3) Home relaid out: status bar → rooms edge-to-edge with thin floor slabs → incident drawer; FILE WORK ORDER FAB floats above the collapsed drawer handle instead of occluding it; SafeArea restored so the advisor no longer bleeds under the app bar. (4a) Bash decisions now carry the command text as the diff core end to end — `decision.requested` was dropping `diffCore` for live decisions, so the black panel showed "(no diff)"; restored. (4b) A pending decision at connect lands on ambient first, then the requisition presents after a 500ms beat (no fullscreen slam mid-pairing-transition). (6) Collapsed drawer paints ONLY the manila folder tab + one-line strip as paper — the area beside the tab is transparent, room showing through on both sides. (7) App icon replaces the default Flutter glyph: square steel plate (texture adapted from `anchors/werkz-logo-hero.png`) + the centered vector W (`vector/werkz-w.svg`, not the raster letterform), embossed, rivets inset from the corners so iOS corner-rounding can't clip them; 1024 master in `assets-pipeline/approved/app-icon/`, generated to all iOS/Android sizes via flutter_launcher_icons and verified legible at 60×60. 23 app tests + analyze clean; goldens committed under `app/test/goldens/`.
- **2026-07-17 (task 11c, device bug: pair hang + black camera)** — Two blockers found on device. (1) Hang on "Paired. Opening the workshop…": task-11 onboarding pushes PairingScreen as a route, so when pairing flipped the root's home to the workshop it rendered UNDER the still-pushed camera route. Fix: PairingScreen pops itself on success + a 5s watchdog that surfaces the connection state instead of hanging (silent hangs forbidden, same rule as silent scans). Also added a 5s timeout to the pair HTTP call → clear "workshop unreachable" error instead of an infinite spinner. (2) Black camera preview (scan still worked): mobile_scanner 7.x only wires its lifecycle observer + start/stop when the widget OWNS the controller; the task-10 custom controller skipped that → black preview on iPhone 12. Fix: let MobileScanner own its controller (PairingScreen is pushed fresh each entry, so repair-after-unpair still gets a live camera). Pair() now returns (session, error). New `pairing_nav_test.dart` reproduces the post-pair sequence with a never-connecting WS client: success pops the camera and reveals the workshop (no hang); rejection stays and shows the reason. 17 app tests, analyze clean.
- **2026-07-17 (task 11b, log drawer + mode teaching + floor crop + swipe/stamp rebuild)** — Device-feel pass. (1) INCIDENT LOG is now a bottom drawer (DraggableScrollableSheet, snap collapsed↔0.7): collapsed = one line + manila folder tab, drag/tap → full history newest-on-top. Replaced the fixed strip and full-screen log route. (2) Onboarding teaches the mode requirement (default mode → decisions route; acceptEdits/auto/bypass bypass the workshop) and the autopilot banner now says "Press shift+tab in your terminal until no mode label shows"; added a numbered "restart your CC session after installing" step (pre-hook sessions don't see it). (3) Floor spacing: baked steel frames cropped out of the three room images (safe inset, signage intact) → rooms sit edge to edge with a single thin floor-slab divider, floor chips overlay the top-left corner. Closes the frame-corner-radius open question. (4) Swipe/stamp REBUILT (Rickard's "worse than Tinder" verdict): card tracks the finger 1:1 with rotation about the grip point, tipping-threshold preview, release momentum (flick completes / slow release springs back); payoff — the card flies off while a FULL-SIZE APPROVED/DENIED stamp slams onto screen center (heavy haptic + sound), holds ~600ms fully readable, then fades. Never truncates the word. Settings: "reduced effects" accessibility toggle. 85 daemon + 15 app tests (+momentum, +reduced-effects, overlay drags retuned for the test surface). Real device is the only valid gate for feel.
- **2026-07-17 (task 11, onboarding + UI fixes + work-order dispatch)** — More real-device findings. (A) Pre-pairing onboarding: app no longer opens straight into the camera — a "REPORTING FOR DUTY" screen explains Werkz + numbered 1955 steps (copyable `npx werkz`, "requires Node 22+ and Claude Code"); camera opens only on "SCAN THE PAIRING REQUISITION". (B) iPhone-12 UI bugs: stacked view now respects safe area (room-label chips clear the status bar); key-save feedback is inline under the field (never a covering sheet); settings right-edge overflow fixed (narration status is a full-width block, not a header chip; rows flex); "Mode: unknown" row hidden until the daemon actually knows the mode. (B4) Narration status block: "● NARRATION ACTIVE — key ending …XXXX" / "○ No key — templates only" (daemon reports last4, never the whole key). (C) WORK ORDER dispatch — the missing initiate-loop: FILE WORK ORDER FAB → Form 17-B sheet (one directive + DISPATCH, "runs on your key/subscription"); daemon `POST /work-order` spawns ONE headless `claude -p` job in the project root whose PreToolUse flows through the SAME decision pipeline (destructive rules identical), appears as a dispatched worker, Stop → job.completed with a game-safe summary; cap 1 concurrent. 85 daemon (+work-order cap/dispatch, +key last4) · 15 app (+onboarding safe-area smoke, +key-status states, +work-order cap) tests. E2E: real work order dispatched → job.completed {ok, turns} in the log; cap + empty-directive rejects verified.
- **2026-07-17 (task 10, pairing lifecycle + key feedback + stacked layout)** — Real-device bugs. (1) Repair-after-unpair was broken: unpair now (a) tells the daemon to revoke the session + reissue a fresh one-time token (`POST /unpair`), (b) wipes stored token/key, (c) rebuilds a FRESH PairingScreen whose scanner controller is created in initState/disposed in dispose. Scanner shows visible feedback on ANY scan (read / unreadable / rejected-with-reason). Daemon: `pair` reports WHY it rejected (logged + returned), `werkz qr` subcommand (+ SIGUSR2, pid file) reissues a QR without restart. (2) Key entry now has feedback: saved → daemon-confirmed → "NARRATION ACTIVE" chip + a `key.accepted` event narrated through Haiku as proof-of-life; offline save queues locally and retries on reconnect. (3) Interim stacked building (pre-Flame) — canon order advisor-penthouse / workshop-floor / archive-basement, letterboxed per room (no logo crop), active room lit; sprites are slice 2. Building order documented in event-model §2. 82 daemon (+revoke/reissue/reject-reason) + 10 app (+unpair→repair, +key-feedback states) tests. E2E verified: old token → 401 w/ reason, unpair → reissue → repair with fresh token, `werkz qr` reprint, key.accepted emitted.
- **2026-07-17 (task 9, first-run + ambient + BYOK narration)** — Real-device feedback: post-pairing was a static screen with no guidance and no way to add a key. Fixed: (1) first-run flow — 3 1955 cards (workshop is calm by design / practice-swipe a mock requisition / optional Haiku key), shown once, skippable, replayable from settings; (2) ambient tap targets — log strip → full history, status bar → settings (connection/session/mode, key management, unpair); (3) room-crop letterbox (BoxFit.contain on oil-gray) so the WERKZ wall logo isn't halved — real stacked view waits for Flame. BYOK narration wired end to end: daemon key store (local only, gitignored `.werkz/narration-key`, 0600, never past the daemon), `POST/GET /narration-key[/status]` over the paired channel, `NarrationEngine` narrates decision/session events via `claude-haiku-4-5` (raw fetch, verified against docs; sanitizer §4.3 sends only categories/counts) and emits `narration.ready` referencing the source event — fire-and-forget, never on the routing path (§3.5); app renders the real line over its template. Daemon exposes narration status in `/status`. 80 daemon tests (+key store, +engine gating), 7 app tests (+narration upgrade), analyze clean. Gate needing Rickard's key + device: a REAL narrated line in the log. Details in `app/DEMO-task9.md`.
- **2026-07-17 (task 8b, first real-device bug)** — First bug found on a real phone: app crashed right after a successful QR pair. Cause: `WorkshopController.build()` started the daemon connection synchronously, and `DaemonClient._open` fired its first state change during `connect()` → re-entered the notifier's own `state` before build() returned → "Tried to read the state of an uninitialized provider". Fix (not a hack): build() returns initial state and defers `_connect` to a `Future.microtask`; every client callback is guarded by a `_disposed` flag; `DaemonClient._open` emits its first state change via `scheduleMicrotask` so it's never synchronous within connect(). Regression test `pairing_connect_test.dart` injects a client that fires onState synchronously on connect() with a pre-stored pairing — reproduces the exact crash (verified: fails with the old code, throwing the identical error) and passes with the fix. CI missed it because `daemon_loop_test` pairs before the provider tree builds. analyze clean; 6/6 app tests; daemon 76/76.
- **2026-07-17 (task 8, app moment loop — P2 slice 1)** — Flutter app: pair → connect → decide, end to end, nothing else. Pairing (mobile_scanner QR + manual paste, session token in flutter_secure_storage); DaemonClient speaks the WS protocol (hello/replay/heartbeat/backoff-reconnect); Riverpod state. The requisition overlay built with love: slides up, 1955 layout, **diff core always visible** (monospace, +green/−red), swipe right=APPROVED / left=DENIED stamp slam, haptics + synthesized stamp.wav, <1s. Ambient screen = approved workshop-floor still + dry-template "INCIDENT LOG" strip (no Flame). Autopilot banner when the daemon reports a permissive mode. Daemon change: exposes permission mode in ws welcome / /status / live session.mode event; and now sends `diffCore` in the decision payload — device-zone only (§4.3), never persisted (stripped before pending.json), never shared. flutter analyze clean; 5/5 app tests incl. a live-daemon integration loop (spawn daemon → pair → WS → held destructive PreToolUse → release deny → hook response deny). Daemon 76/76. Gate needing hardware: on-device video (Rickard). Details in `app/DEMO.md`.
- **2026-07-16 (task 7, LAN WS channel)** — The live channel the Flutter app will speak. `/ws` on the daemon port, session-token auth at upgrade, versioned JSON protocol (hello/welcome), heartbeat ping/pong. Server→client: event stream as bus events land, pending snapshot on connect, replay-from-eventId on reconnect (uuidv7 time-ordered + JSONL). Client→server: decision release, severity subscribe filters. Persistence: paired session tokens and open decisions survive a daemon restart — orphaned holds emit `decision.superseded` (reason daemon-restart, no trust effect), never silently lost. Broadcast is fire-and-forget from the bus (never blocks routing, §3.5). fake-phone upgraded to WS (HTTP polling kept behind --http). 76 tests (+WS protocol, +restart recovery). E2E: two surfaces share one decision consistently; kill -9 mid-hold → restart recovers cleanly, session token still valid. Caveat surfaced: under acceptEdits, CC's fallback when the daemon is absent ACCEPTS — permissive modes decide without you if the workshop is closed. Demo in `daemon/experiments/ws-channel-demo.md`.
- **2026-07-16 (task 6, daemon bootstrap)** — `npx werkz` "first 60 seconds", daemon side: startup + terminal QR (host/port/one-time token) + friendly 1955 status line; hook injection into project `.claude/settings.json` that MERGES (preserves user hooks), is idempotent, and `npx werkz uninstall` removes exactly ours — verified byte-identical restore. projectId now derives from git identity (origin remote normalized, else repo-root path hash): worktrees/branches/clones = same workshop, multiple sessions = multiple workers, always free; multi-repo = paid boundary (event-model §1, P4 note). Pairing/auth: one-time QR token → POST /pair → session token; decision/state endpoints require it; /dev/* only behind WERKZ_DEV=1. mDNS advertise (_werkz._tcp) best-effort. scripts/fake-phone.mjs stands in for Flutter. 68 tests. E2E demo (fresh repo → pair → held destructive decision denied through paired channel → clean uninstall) in `daemon/experiments/bootstrap-demo.md`.
- **2026-07-16 (task 5, event-model v0.3 + adapter slice)** — Amendment folded in: decision hook is `PreToolUse` (PermissionRequest demoted — never fires headless), hold ceiling fixed at open (§3.4), new §3.5 latency budget (<50 ms non-decision path, release-blocker tier) and §3.6 dedup rule. CRITICAL ASSUMPTION box → RESOLVED with two hardware gaps as P1 exit criteria. Then built the first real daemon slice under `daemon/src`: config, event bus (JSONL), classifier→router (classes + trust thresholds), dedup store, pending-hold manager, trust store, CC adapter (PreToolUse → WerkzEvent, game-safe payloads), http server (`/pretooluse`, `/release`, `/dev/trust`), runnable CLI. 54 tests pass incl. p99<50 ms latency and retry-after-deny dedup. Demo evidence: real CC 2.1.187 sessions — read auto-allowed, destructive `echo > tracked` HELD → denied → CC blocked (file unchanged), routine auto-allow logged at 2.47 ms. Slice does NOT do LAN/narration/trust-lifecycle yet.
- **2026-07-16 (task 4, hold prototype)** — CRITICAL ASSUMPTION tested against real CC 2.1.187. VERDICT: viable with constraints. Held an http permission hook **32 min continuous → released allow → tool actually executed**; timeout honored far past the 600 s default (7200 set, 20+ min observed live). Deny blocks; timeout-overrun and server-kill both degrade gracefully (session always survived). Big correction: `PermissionRequest` does NOT fire in headless `claude -p` (4 runs, never reached the server) — build on `PreToolUse` instead (fires both modes, gates execution, same hold plumbing). §3.4 "no fixed timeout" isn't literally possible: ceiling is set when the hook opens. Also found: model retries a denied tool → hook re-fires → daemon must dedupe. Results doc written; §3.4 amendment pending Rickard. Untested-needs-hardware: full multi-hour wall-clock + sleep/wake.
- **2026-07-16 (task 3, P0 exit)** — event-model v0.2 fully approved (task 2 closed). Monorepo scaffolded: daemon/ (Node 22+/TS/ESM, `npx werkz` bin, WerkzEvent types verbatim from event-model §1, data-driven destructive classifier with 39 passing tests, branded narration zone types with no cast path, server stubs), app/ (Flutter portrait-locked, Flame + Riverpod declared, analyze clean), backend/ (sql/ history convention, no-CLI rule documented), landing/ (Astro + Tailwind v4 placeholder), root README + CI (daemon typecheck + tests). No feature code — scaffold means scaffold. P0 exit criteria met; "Now" #1 is the hold prototype per the CRITICAL ASSUMPTION box.
- **2026-07-16 (task 2 amend, v0.2)** — Gate feedback applied: adaptive hold strategy (hold-until-decided when phone connected + terminal quiet; 300s only at keyboard; never block unreachable), long-hold risk promoted to CRITICAL ASSUMPTION box gating all daemon work. 48h expiry clock now counts workshop-open hours only. Destructive list +3 categories (VCS data-loss, cloud/container destruction, redirection overwrites) — catch-all kept last. Octagon cost policy decided into §5 (hint always, user-initiated always, auto-suggest 2/day). Re-review pending on §3.4 only.
- **2026-07-16 (task 2 draft)** — event-model.md v0.1 written after verifying current CC docs (hooks API now has 31 event types incl. PermissionRequest, PostToolUseFailure, SubagentStart — and http hook handlers, which make the daemon adapter clean: hooks POST straight to localhost). Taxonomy maps 20+ CC sources to internal events; destructive-op list proposed (8 categories); sanitizer designed as two type-separated zones — free narration can never reach share cards. Honest gap flagged: remote decisions ride a synchronously-held PermissionRequest hook, prototype first in P1. Gate: Rickard reviews before daemon/ starts.
- **2026-07-16 (task 1 closed)** — Room gate approved. Empty rooms 02–05, advisor office (standard + idle) moved to `approved/`. New anchor rules: off-grammar bureaucratic English allowed sparingly in minor signage only; octagon benches populated by sprites at runtime. Open question added on room frame uniformity (P2 decision). Task 1 done — next: event-model.md.
- **2026-07-16 (1b-fix)** — Canon locked: robot workers (three personnel-file personas), human advisor as room fixture, rooms always unpopulated (sprites composite at runtime). Populated first-batch rooms parked as P2.5 marketing stills in `output/marketing/`. Rooms 02–05 regenerated empty + advisor idle variant (newspaper, coffee): 5/5, verified — no figures, walkable floor bands, all signage correctly spelled with proper W. The generator improvised good bureaucracy ("DOCUMENTS ARE EVIDENCE OF EFFORT", "WERKZ CODE OF ORDER"). Awaiting Rickard's room gate → task 1 closes.
- **2026-07-15 (night)** — Vector gate APPROVED by Rickard: `vector/werkz-w.svg` is the canonical mark. Batch 1b–1d generated, 7/7: five rooms, decision overlay, worker sprites. Diff panel in the overlay verified abstract (no readable code strings — sanitizer rule holds even in concept art). Workers came out as riveted robots; advisor as human NPC. All in `output/` (ungitted) pending Rickard's pick.
- **2026-07-15 (night, 1a-vector)** — Hero W vectorized by geometric construction, not autotracing: edge-map measurement of the letterform crop (bbox 224:122, apex at 34% height, notch depth ~64%, dual-weight strokes 46/32), then a hand-built 13-point path. The AI raster turned out asymmetric (photo perspective + generative wobble); rationalized to a clean symmetric mark. `vector/werkz-w.svg` + knockout + 1024/48px renders + `comparison.png`. Gate: Rickard approves comparison.png before the SVG ships anywhere.
- **2026-07-15 (evening, 1a-final)** — Hero mark locked by Rickard: variant 05 "Embossed Metal Plate" from the candidate-3 sheet (variants are picked, not sheets). Plate cropped to `anchors/werkz-logo-hero.png`, letterform isolated to `werkz-logo-hero-letterform.png` (vectorization source). Embossed plate designated as app-icon rendering; candidate-1 sheet demoted to treatment reference. Shared prompt prefix now points at the hero letterform.
- **2026-07-15 (evening, 1a-fix)** — Anchor v1 approved by Rickard, with one correction: the W's center apex rose too high (reads as four bars small). Standalone logo sheet generated (3 candidates); winner has the low center vertex consistent across all 6 variations incl. stencil-cut and small-size row. `anchors/werkz-logo-v2.png` now supersedes the anchor-v1 W for all assets; anchor-v1 stays canonical for materials/palette only. Open task: manual vectorization of final logo (raster AI not acceptable for app icon). Logo rule added to shared prompt prefix.
- **2026-07-15 (evening)** — Task 1a done: WERKZ style anchor v1 generated (gpt-image-2 via Fal, 3 candidates, tile-level text inspection). Winner has flawless typography: correct WERKZ branding throughout, all 10 palette hex codes exact, zero Lumon residue. Canonical anchor + prompt + governance README in `assets-pipeline/anchors/`. Asset pipeline (`generate.mjs`, manifest, room/overlay/sprite prompts) committed earlier today. Q.A. Division has stamped it; Rickard has not — style gate still open.
- **2026-07-15 (later)** — Marketing phase P2.5 added: 1955-conglomerate brand play, X-first channel plan, waitlist gate ≥500 before beta.
- **2026-07-15** — Project born from a midnight TikTok. GDD designed and approved through all 8 phases (game-mechanics-designer skill). Name set: Werkz. Payment model pivoted from IAP to web-only after anti-steering research. Masterplan, this doc, and CLAUDE.md created. Next: concept art.
