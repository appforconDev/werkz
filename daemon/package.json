{
  "name": "werkz",
  "version": "0.0.1",
  "description": "Werkz daemon — turns your coding agents' work into a workshop you run from your phone.",
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "bin": {
    "werkz": "./src/cli.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test --test-force-exit --test-timeout=15000 'src/**/*.test.ts'"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/ws": "^8.5.12",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "qrcode-terminal": "^0.12.0",
    "ws": "^8.21.1"
  },
  "optionalDependencies": {
    "bonjour-service": "^1.3.0"
  }
}
