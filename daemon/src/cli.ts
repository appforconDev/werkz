#!/usr/bin/env node
// `npx werkz` entrypoint. Runs directly as TypeScript via Node's native
// type stripping (engines: >=22.18). TODO(pre-publish): compile to dist/
// and point bin there instead.

console.log('WERKZ INDUSTRIES — daemon v0.0.1 (scaffold)');
console.log('Nothing is operational yet. The workshop opens in P1.');
