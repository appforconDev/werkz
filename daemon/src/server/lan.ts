// LAN discovery + pairing — mDNS/Bonjour advertise, QR pairing token,
// WebSocket event stream to the app. Free mode never touches our backend.
// TODO(P1): implement per MASTERPLAN P1. Intentionally empty in scaffold.

export {};
