.DS_Store
.env
.env.*
!.env.example
node_modules/
dist/
output/
*.log
build/
.werkz/
app/.dart_tool/
app/build/
app/.flutter-plugins*
app/ios/Flutter/ephemeral/
app/**/Pods/

# Bind-pose render is a test artifact (regenerated each run); keep only the diff.
assets-pipeline/sprites/parts-hires/*/_bindpose-render.png
assets-pipeline/sprites/parts-hires/*/_phase-*.png

# Clipboard generation attempts are bulky (task 19l); ref.png + chosen.png are the
# reproducible inputs the cut pipeline uses — keep those, drop the raw candidates.
assets-pipeline/sprites/clipboard-candidates/*/attempt*.png
assets-pipeline/sprites/crew-front/
assets-pipeline/sprites/beam/attempt-*.png
assets-pipeline/sprites/beam/_ref.png
